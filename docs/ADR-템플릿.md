# ADR-NNN — 제목

날짜: YYYY-MM-DD · 상태: **결정 · 미구현** / **채택 · 구현됨** / **번복됨 (ADR-MMM)**
관련: ADR-XXX · §NN · LOCK N

---

## 문제

무엇이 왜 아팠나. **실측을 인용한다** — "느낌상 느리다"가 아니라 "80,138행에서 9.3초".

## 조사 — 남들은 어떻게 하나

버린 접근을 **이유와 함께** 적는다. 이 절이 ADR의 진짜 값이다 — 결론만 남기면
다음 사람이 버린 길을 다시 판다.

## 결정

---

## ★ 파급면 — 이 결정이 닿는 표면 전수 ★

> **이 절이 비어 있으면 그 ADR은 미완성이다.**
>
> 2026-08-20 감사가 같은 구멍을 **세 번** 찾았다. 결정 자체는 셋 다 옳았는데,
> **결정의 파급면을 세는 단계가 없어서** 기존 원칙이 새 자리에 다시 적용되지 않았다:
>
> 1. `removeCost`를 열 때 ADR-016의 「다리 사전」에 같은 질문을 안 던졌다 →
>    「지운 원가가 되살아난다」가 **코드 주석 한 곳에만** 산다
> 2. ADR-018이 웹판을 열 때 `pickFile`·배너만 훑고 `driver-wasm.ts`를 안 훑었다 →
>    **번복된 옛 결정이 코드에 생존**했고, 그 주석이 참조하는 `isWasm`은 존재하지도 않는다
> 3. ADR-019가 「대기 목록으로 남긴다」고 약속했는데 → **그 목록에 항목이 없다**

### 무엇이 함께 움직이나

| 층 | 닿나 | 무엇 |
|---|---|---|
| 스키마 | | 새 표·컬럼·뷰 재정의 |
| 조회 | | `active_*` 뷰를 지나는가 · 기준 표를 직접 읽는가 |
| 화면 | | 새 vals · 새 구간(`data-s21`) · 기존 문구가 거짓이 되나 |
| 도구 | | 하네스·CLI가 같은 규약을 타나 |
| 게이트 | | 어느 시험이 함께 움직이나 |

### ★ LOCK 재적용 — 열 줄을 한 번씩 묻는다 ★

| LOCK | 이 결정이 이 규칙을 새로 위반할 수 있나 |
|---|---|
| 1 Fact 공통 6컬럼 | |
| 2 append-only | |
| 3 원본 불변 | |
| 4 Core는 마켓을 모른다 | |
| 5 메모리 적재 금지 | |
| **6 조용한 실패 금지** | ← **여기서 세 번 다 뚫렸다.** 새 상태·새 경로가 생기면 화면이 그것을 말하는가 |
| 7 AI는 선택 | |
| 8 내보내기 개방 | |
| 9 인라인 편집 금지 | |
| 10 「동기화」 카피 금지 | |

### 사용자 불변식 재적용

[docs/사용자-불변식.md](사용자-불변식.md) U-1~U-8을 같은 방식으로 훑는다.
특히 **U-1(넣은 것은 장부에 보인다)** 과 **U-5(저장 분류는 화면의 언어가 아니다)** —
새 저장 경로를 여는 ADR은 이 둘을 반드시 답해야 한다.

### 앞선 결정을 뒤집나

뒤집는다면 **양쪽 문서에 적는다.** 안 적힌 번복은 번복이 아니라 사고다
(`CLAUDE.md` 문서 우선순위).

- [ ] 이 ADR에 「무엇을 뒤집었나」
- [ ] 뒤집힌 ADR에 「ADR-NNN이 뒤집었다」
- [ ] 그 결정의 **자취**(코드 주석 포함)를 전수로 훑었나 ← ADR-018이 여기서 놓쳤다

---

## 따름 조건

이 결정이 참인 동안 지켜져야 하는 것. 가능하면 **시험으로 승격**한다.

## 재검토 트리거

무엇이 일어나면 이 결정을 다시 여는가.

> ⚠ **트리거가 발동하면 이 문서로 돌아온다.** 2026-08-20 감사에서 발동한 줄
> 알면서 문서를 안 고친 트리거가 셋 있었다 (ADR-010 트리거 3 · ADR-007 트리거 4 ·
> ADR-011 트리거 1). 발동한 트리거를 안 닫으면 그 문서는 **거짓을 현재형으로 말한다.**

## 예약한 측정

측정을 예약했으면 **결과를 이 문서로 되돌린다.**

> ADR-008이 「Tauri IPC는 아직 측정되지 않았다」로 6일간 열려 있었다 — 측정은
> 2026-08-13에 실제로 수행됐는데 결과가 ADR로 안 돌아왔다. 그 사이 이 자리는
> 「살아 있는 위험」으로 계산됐다.
