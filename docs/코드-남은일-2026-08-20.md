# 코드에 남은 일 — 2026-08-20 전수

⚠ **한시 문서 — 소비되면 지운다.** [문서-감사-2026-08-20](문서-감사-2026-08-20.md)과 같은 성격이다.

**만든 방법**: 6개 렌즈로 코드를 동시에 훑고(컷·코드 속 표식·불변식 래칫·저장 계층·§조항 대 코드·게이트),
각 렌즈 결과를 「HEAD에서 정말 열려 있나」로 다시 검증했다. **문서가 아니라 코드가 정본**이다 —
이 저장소의 문서는 같은 날 두 번 거짓으로 드러났다(「실기기 DB 13」→실측 11, 「리스팅 190건 미연결」→실측 0).

> ## ⚠ 조사 자신의 낡은 분류를 여기서 정정한다
>
> 원 조사는 **「원본 보관층」을 「의도적 컷 — 지금 안 하기로 정한 것」** 표에 넣었다.
> 그 분류는 **하루 만에 낡았다** — 같은 날 저녁 [ADR-023](ADR-023-통-묶음-자격-입구순서.md)이
> **그것을 앱의 척추로 올렸다.**
>
> 같은 이유로 조사 2.1의 처방(「`batch.connection_id NOT NULL`을 (a)완화 (b)합성 connection
> (c)UNION 중 무엇으로 풀지 정해라」)도 **셋 다 답이 아니다.** ADR-023이 batch를 척추로
> 삼는 프레임 자체를 기각했다.
>
> 아래 A/B/C 분류가 **정본**이다.

---

## 목차

| 절 | 무엇 | 건수 |
|---|---|---|
| **A** | 방향 — ADR-023이 정한 것. 확인 질문 4개가 선행 | 5 |
| **B** | A가 자연히 흡수하는 것 — 따로 세지 않는다 | 3 |
| **C-1** | 앱이 지금 사실이 아닌 말을 하거나 고장으로 읽힌다 | 10 → **남은 4** |
| **C-2** | 사용자가 못 하는 일 | 10 |
| **C-3** | 조항이 선언했는데 코드에 없다 | 10 → **남은 9** |
| **C-4** | 게이트가 거짓 안심을 준다 | 12 |
| **D** | 의도적 컷 — 지금 안 하기로 정한 것 | 21 |
| **E** | 이미 닫혔는데 문서·주석이 안 따라온 것 | 12 |

---

# A. 방향 — [ADR-023](ADR-023-통-묶음-자격-입구순서.md)

> **상태: 제안 · 사용자 확인 대기.** ADR-023 맨 아래 「확인이 필요한 것」 4개에 답을 받기 전에는
> 스키마를 만들지 않는다.

| | 무엇 | 크기 |
|---|---|---|
| **A-1** | **통을 세운다 (얕은 층)** — 넣은 파일은 전부 한 줄. batch는 그 줄에 달리는 결과 중 하나 | L |
| **A-2** | **통의 깊은 층** — 파싱된 표를 보관하고 **표로 다시 본다** | L |
| **A-3** | **묶음이 시각화의 자격을 갖는다** — §22 판정 단위를 「연결」에서 「묶음」으로 올린다 | L |
| **A-4** | **입구 순서 뒤집기** — 가져오기 → 통 → 묶음 → 대시보드. 지금은 출력이 입구에 있다 | M |
| **A-5** | **API는 통의 새 입구** — 새 층이 아니다. 지금은 설계만 | — |

# B. A가 자연히 흡수하는 것 — 따로 세지 않는다

| 조사 항목 | 흡수처 | 왜 |
|---|---|---|
| 2.1 원가가 장부에 안 남고 되돌릴 수 없다 | **A-1** | 「원가도 batch를 쓰게」가 아니라 파일 장부가 답이다 |
| 2.12 `collection_event`가 쌓이는데 읽는 화면 0 | **A-3** | 묶음 화면이 커지면 자리가 생긴다 |
| 1.4 「내 필드」가 백지 | **A-2** | ★ 기능이 부실한 게 아니라 **얹힐 표가 없다.** 통이 생기면 산다 |

---

# C-1. 앱이 지금 사실이 아닌 말을 한다

### 1.1 ~~정산 「일치」가 금액을 보지 않는다~~ ✅ **닫혔다** (`7bbfba2`)

> ⚠ **이 항목의 처방이 틀렸었다.** 「이미 만들어 둔 `recon.ts`를 잇는 일」이라 적었는데,
> 열어 보니 **거기에 금액 대사가 없다** — `reconKey`(식별자)와 `withinPeriod`(기간 비교)뿐이다.
> 대신 **항등식**(`지급액 =? 판매액 − 수수료 − VAT − 배송비`)을 검사한다. 실 DB 128행 전부 성립 →
> 오늘은 조용하고 **깨지는 날 말한다**(11번가 도서산간배송비 = 2.13이 그 형태다).
> **여전히 없는 것: 「정산서 금액 vs 우리가 계산한 금액」 대사.** 조회가 는다(core).

<details><summary>원문</summary>

`src/app/settlement.ts:152` `const linked = r.linked === r.count` → `:175` `recon: linked ? "일치" : "주문 미연결"`.
판정 근거가 **「주문에 이어졌나」 하나**인데 화면 부제는 「마켓 정산서 대사」다. 정산서 금액과 우리
금액이 달라도 「일치」라고 말한다.

`src/core/accounting/recon.ts`의 `reconKey`·`withinPeriod`를 import하는 곳은 `tests/accounting.test.ts:17`
**하나뿐**, `src/` 안에는 0곳. 그런데 `docs/ADR-009:310-315`의 적용 지점 표는 ④ 대사 키를 **구현됨**으로 적었다.

</details>

### 1.2 ~~「지금」이 전부 UTC다~~ ✅ **닫혔다** (`17af87e`)

> 조사는 네 자리를 짚었는데 **시험이 다섯째를 잡았다**(`App.tsx:225` `monthRef`).
> `nowStamp`는 **UTC로 남겼다** — 트리거의 `datetime('now')`(UTC, 21곳)와 같은 시계를 써야 한다.
> 규칙이 갈렸다: **사람이 읽는 날짜 = KST · 기계가 정렬하는 시각 = UTC**.
> 곁가지로 `vitest.config.ts`에 `__PROJECT_ROOT__`를 줘서 **`data.ts`를 시험에서 열 수 있게** 했다(4.7 일부).

<details><summary>원문</summary>

`src/app/data.ts:183` `new Date().toISOString().slice(0,7)` · `:263` `today()` · `:486` `nowStamp()` ·
`src/app/App.tsx:199` 기본 달.

ADR-009 293~307행: 「기간 경계는 **KST(UTC+9) 자정**」·「`Date` 객체를 만들지 않는다」.
파서(`parseDateLike`)만 그 규율을 지키고 **「지금」을 만드는 다섯 자리가 전부 UTC다.**

결과: **매월 1일 아침 00~09시(KST)에 열면 지난달 손익이 뜬다.** 그 시간대에 저장한 원가는
`effective_from`이 전날로 박힌다.

</details>

### 1.3 ~~가짜 드롭존~~ ✅ **닫혔다** (`f527939`)

> `document`에 걸었다 — 드롭존에만 걸면 **빗나간 드롭**이 그대로 위험하다.
> **파일 유무를 보기 전에** `preventDefault` — 링크·텍스트 드롭도 기본 동작이 내비게이션이다.
> 곁가지: 헌장 C-6 가드를 **좁히고 부러뜨려 확인했다**. 처음엔 안 물었다 —
> `ondragstart`(소문자)와 `dataTransfer?.setData`(옵셔널 체이닝) 두 구멍.

<details><summary>원문</summary>

`Template.tsx:534` 「정산 파일을 여기에 놓으세요」 · `shell.ts:61` 온보딩 문구.
저장소 전체에 `onDrop`/`onDragOver`/`onDragEnter` **0건**.
`styles/vector-app.css:641` `.imp-dropzone.over { … }` — 켤 클래스는 준비돼 있고 켜는 코드가 없다.

★ **위험** — `src-tauri/tauri.conf.json`의 `dragDropEnabled: false`라 드롭이 웹뷰로 그대로 가고
`preventDefault`가 없다 → **웹뷰가 그 파일로 내비게이션해 앱이 통째로 사라질 수 있다.**
첫 사용자가 첫 동작에서 만난다.

</details>

### 1.4 설정·데이터 구조·내 필드가 「준비 중」조차 없이 백지로 열린다 — M · ✅

`src/app/shell.ts:270` `export const UNBUILT: readonly NavKey[] = ["diag"]` — `NAV`는 14개인데 안내를 받는 것은 하나다.

- **설정**(`Template.tsx:5866`): `vals.setSections`가 빈 배열, `ssLic`/`ssGen`/`ssData`가 `vals.ts:1383-1385`에서 전부 `false`. 렌더 텍스트 **0자**
- **데이터 구조**(`:5744`): 제목 셋은 조건 없이 그려지고 `pipeline`·`entities`·`formula` 세 표가 빈다
- **내 필드**(`:5540`): 설명 문단 + 무동작 [필드 추가] (→ B: A-2가 푼다)

`src/app/palette.ts:98` `if (UNBUILT.includes(key)) continue`인데 UNBUILT가 diag 하나라
**⌘K가 이 셋을 전부 목록에 올린다.** 같은 파일 `:18-20`이 「골라서 갔는데 「준비 중」이면
팔레트가 거짓말한 것」이라 적어 뒀는데, 지금은 **「준비 중」조차 없는 백지**로 보낸다.

⚠ **「`UNBUILT`에 한 줄 더하면 끝」이 아니다.** unbuilt 안내 마크업은 `Template.tsx:2492`의
`data-s21="unbuilt-diag"` 하나뿐이라 ① vals 필드 신설 ② 생성물에 구간 삽입
③ `tests/convert-gate.test.ts:979`식 「배선 시 제거」 선언 ④ `shell.ts:272` `applyUnbuilt` 로직이 함께 필요하다.

### 1.5 ~~⌘K 팔레트 안을 마우스로 누르면 닫힌다~~ ✅ **닫혔다** (`8558cd0`)

### 1.6 눌리는데 아무 일도 안 일어나는 컨트롤 — M · ✅ · ⚠ **부분 이행**

`onClick={vals.X}` 고유 키 67개 중 배선 31 · 무동작 36이었다.

**닫힌 것** — `openCmd`·`stop`·`stopEvt` (`8558cd0`) · 채널 상단 데모 줄과 빈 버튼 (`d961b13`).
wiring: 배선 246 → **249** · 컷 223 → **220**.

**남은 것과 왜 아직 못 하나** — 갈래가 넷이다:

| 갈래 | 무엇 | 막고 있는 것 |
|---|---|---|
| 멈춤 | 대시보드 「계산 과정」 · 상품별 표 정렬 · 필터 · 표시 설정 | **사용자가 멈추자고 했다** (2026-08-20) |
| 선행 | 「내 필드」 [필드 추가] | **ADR-023 A-2(통)** — 얹힐 표가 없다 |
| 화면 없음 | 채널 [연결 관리] | 연결 관리 화면 자체가 없다 |
| 탭이 하나 | 비용 탭 · 가져오기 출처 탭 | 전환 대상이 없다 → **2.6(광고비 탭)이 비용 탭을 살린다** |
| 변환기 | 내보내기 「숨긴 컬럼도 포함」 4곳 · onClick 없는 버튼 11개 | 라벨이 있어 `.v-btn:empty`로 못 잡는다. **생성물 마크업에서 빼려면 변환기 경로**(`tools/convert/` + `DEVIATIONS` 선언)다 — 별건의 큰 일 |

> ★ **그중 「오류 행 내려받기」(`Template.tsx:4597`)는 지우지 말고 배선할 값이 있다** —
> `batch_issue`에 데이터가 있고 내보내기 기계도 이미 있다 (LOCK 6 + LOCK 8).

원래 목록:

| 자리 | 파일:줄 |
|---|---|
| 대시보드 히어로 「계산 과정」 | `Template.tsx:1300` ← `vals.ts:1086` `heroBasisGo: () => {}` |
| 「내 필드」 primary [필드 추가] | `:5552` ← `vals.ts:849` `addCf: () => {}` |
| 채널 카드 [연결 관리](카드마다) | `:4037` ← `channel.ts:227` `manage: () => {}` |
| 채널 상단 **빈 세그먼트 + 라벨 없는 빈 버튼** | `:3941`·`:3951` (`stateTabs`·`firstRunLabel` 미배선) |
| 대시보드 상품별 표 헤더 10열 정렬 | `dashboard.ts:259-269` `pick: () => {}` (`:1611`이 `cursor:pointer`를 상수로 박는다) |
| 비용 탭 · 가져오기 출처 탭 · 필드맵 pick | `costs.ts:219` · `import.ts:317` · `fieldmap.ts:344` |
| **onClick이 아예 없는 버튼 11개** | `:542`(파일 선택) `2318`(SKU 등록) `4268` `4287`(행 추가) `4297`(3건 확정 — **목업 시드 숫자가 문자열로 박혀 있다**) `4597`(오류 행 내려받기) `5908` `6030` `6130` `6134` `6497`(연결 테스트) |
| **내보내기 「숨긴 컬럼도 포함」 체크박스 4곳** | `:2026·2338·3106·3790` ★ **오늘 팝오버를 살려서 이제 눈앞에 뜬다** |

⚠ 마지막 줄은 U-3 게이트가 **구조적으로 못 본다** — `tests/user-invariants.test.ts:217`이
`onClick={vals.…}` 패턴만 훑는다. 행 객체 하위 핸들러(`manage`·`pick`)도 안 세어진다.

### 1.7 표 5곳의 행이 「누르면 나온다」고 말하는데 갈 곳이 없다 — L · ✅

배선: `dashboard.ts:303`·`:362` · `order.ts:120` · `product.ts:206` · `settlement.ts:218` 전부 `click: () => {}`.
마크업: `Template.tsx:1626`·`1802`·`2132`·`2413`·`3886` 전부 `<tr className="fb-row" style={{cursor:"pointer"}} onClick={r.click}>`.

`product.ts:205`는 「상세 패널은 아직 없다. **어포던스를 그리지 않는다** (§21-1)」라 적었는데
어포던스는 Template에 하드코딩돼 있다. CSS에 `.fb-row:hover`가 없어 이 커서가 유일한 어포던스다.

**막힌 원인은 하나** — §21-2 L2 근거 패널 배선이 0이다. `vals.ts:1078` `hasDetail: false`가 유일한 대입이고
`Template.tsx:6245` `{vals.hasDetail && (`의 `<aside className="detail-side">`는 **한 번도 렌더된 적이 없다.**
`tests/user-invariants.test.ts:384` `DEAD_ROW_CLICK_MAX = 5`가 이것을 「갚을 빚」으로 센다(컷 선언이 아니다).

(여섯 번째 같은 모양인 가져오기 기록 `:4936`은 `history.ts:255`로 **살아 있다.**)

### 1.8 음수 금액을 막는 자가 아무도 없다 — L · ❌

선언된 집행자 셋이 전부 안 돈다:

1. `src/core/accounting/sign.ts:34` `assertStoredPositive` — **프로덕션 호출부 0곳**(시험뿐).
   같은 파일 `:31`은 「스키마에 CHECK가 없는 것은 LOCK 때문이며 **이 함수가 그 자리를 대신한다**」고 적었다. **거짓이다**
2. 프로파일 9장이 `"validationRules": [{"kind":"nonNegative"}]`를 선언 — 읽는 코드 0건 (→ 3.1)
3. 마이그레이션 001~014에 `CHECK (… >= 0)` 0건

실제로 도는 유일한 집행은 `mapping/index.ts:961` `if (m.signPolicy === "magnitude")`이고 **선언한 매핑에만** 걸린다.

**정정**: 「조용한 실패」는 아니다 — `sign_undeclared`가 `run.ts:427`→`:479`로 DB에 남고
`import.ts:1002`가 화면에 문장까지 띄운다. 열려 있는 것은 **말은 하는데 막지 않는다**이다.
오늘 픽스처에 음수 0건이라 아직 안 터졌다.

### 1.9 ~~파서 경고를 아무 화면도 안 읽는다~~ ✅ **닫혔다** (`7b98983`)

> 한 배열에 **두 종류가 섞여** 있어서 결국 둘 다 안 보였다. 타입으로 갈랐다 —
> `sheetNotes`(우리 한국어 문장 · 화면에 그대로) · `warnings`(SheetJS 영문 원문 · 화면에 안 냄).
> **원문을 안 내는 것이 LOCK 6 위반이 아닌 이유**를 타입 주석에 남겼다: 거기 담기는 것은
> 제외가 아니라 진단 출력이고, 건수만 내면 손댈 수 없는 숫자로 겁만 준다.

<details><summary>원문</summary>

`analyze.ts:318` 「조용히 자르지 않는다 — 못 본 시트가 있다는 사실을 말한다 (LOCK 6)」 →
`:319` 「시트가 N장이라 앞 M장만 훑었습니다」 · `:334` 「시트 i를 읽지 못했습니다」 → `:480` `warnings: [...]`.
`run.ts:105` 주석: 「**오늘 아무도 표시하지 않는다.**」

`grep -rn '\.warnings' src/app/` → **0건**. `run-reference.ts:93`도 스스로 「소비처는 0이다」라 적었다.
대비: 같은 파일의 `identityNotes`는 `import.ts:377`이 꺼내 쓴다. **이 채널만 빠졌다.**

</details>

### 1.10 데모 DB 404·개인 프로파일 결함이 `console.warn`에만 남는다 — S · ⚠

`data.ts:237` `console.warn("[data] 데모 DB를 받지 못했다 — 빈 DB로 연다")` — 화면은 「웹 데모」 배지를
단 채 빈 앱을 보여준다. **「안 넣었다」와 「못 받았다」가 한 문장으로 뭉개진다.**
`:511`·`:516` — §20으로 확정한 개인 프로파일이 결함으로 건너뛰어지는데 그 수(`broken`)를 화면으로
옮기는 코드가 0건. 바로 위 주석은 「**세어서 경고하고 건너뛴다** (LOCK 6)」라 적었다.

같은 저장소가 `App.tsx:657`에서 원가 저장 실패를 「`console.warn` 하나로 끝났다」며 모달로 고쳤다.
**같은 처방이 여기엔 안 왔다.**
※ `tests/user-invariants.test.ts:92` `WARN_MAX = 3`이고 실측도 3 — **여유 0이다.**

### 1.11 ~~주문 화면 품목이 «—»~~ ✅ **닫혔다** (`ecf38dc`)

> 브라우저 실측: **155행 전부** 상품명·수량이 뜬다. 조인 전에 **주문 단위로 접었다** —
> 안 접으면 행이 품목 수만큼 불어 금액이 중복 계상된다(시험이 그 성질을 문다).
> 부질의 안에도 기간 필터를 걸었다(LOCK 5).

<details><summary>원문</summary>

`src/app/order.ts:101` 「`fact_order_item`이 **비어 있다**」 → **거짓이다.**
`run.ts:364` `await repo.loadChunk("fact_order_item", batch, itemRows)`가 적재한다.

진짜 원인은 조회다 — `core/order/rows.ts:113`의 SQL이 `FROM active_order o LEFT JOIN connection cn
LEFT JOIN active_claim c`뿐이고 `fact_order_item`이 없다. 그래서 `OrderRow`(`:53-94`)에 품목·수량 필드가 없다.

**같은 데이터로 대시보드 상품별 표는 품목 단위로 그려진다 — 사용자는 두 화면 중 어느 쪽이 맞는지 알 수 없다.**
`profit/rows.ts`의 `ITEM_JOIN` 조각을 그대로 빌릴 수 있다.

</details>

---

# C-2. 사용자가 못 하는 일

### 2.6 광고비 탭이 없다 — 컷 사유가 코드 안에서 이미 무효화됐다 — M · ⚠

> ★ **만들어도 오늘은 빈 화면이다** (2026-08-20 실측) ★
> `fact_ad_spend`가 **실 DB 0행 · 데모 0행**이다. 데모는 일부러 뺐고
> (`tools/harness/demo-db.ts:57` 「쿠팡 광고 8만 행은 **뺐다**」), 실 DB에는 아직 안 넣었다.
> 즉 탭을 만들면 **「광고 파일을 넣으면 열립니다」 빈 상태**로 서고, 사용자가 쿠팡 광고
> 파일을 넣는 순간 채워진다. 「눈에 보이는 것 우선」 기준으로는 **선행이 사용자 손**이다.
>
> KPI 넷(`adTotal`·`adCount`·`adAlloc`·`adUnalloc`·`adRoas`)은 **새 조회가 필요 없다** —
> `snapshot.ts:343-356`의 `adSplit`(direct·unallocated·noKey·noListing·noLink)이 이미 있다.
> 새로 필요한 것은 **캠페인 단위 표**(`adRows`) 하나다.
> ⚠ 표의 「배분 규칙」 칸(`a.setRule`)은 **D절의 컷**이다 — ADR-022가 「배분 규칙을 발명하지
> 않는다」고 정했으므로 배선하지 않는다. 그 칸은 «파일이 무엇을 말했나»만 적어야 한다.

`costs.ts:219` `vals.costTabs = [{ label: "운영·고정비", on: "active", pick: () => {} }]` —
목업의 넷 중 하나만 남았고 그 하나의 `pick`조차 무동작이다(전환할 상대가 없어서).

`tools/harness/wiring.ts:94-97`이 **스스로 적었다**: 「「광고비 배분은 재료가 아직 없다」던 사유는
2026-08-20에 **거짓이 됐다** — 014가 `listing_key`를 열어 광고비의 95.3%가 상품까지 내려간다(ADR-022).
남은 것은 «재료 부족»이 아니라 **«그 재료를 보여주는 탭을 아직 안 만들었다»**이다.」

### 2.7 마켓 주문번호를 저장하지 않아 「외부 주문번호」가 상수 «—» — L

`order.ts:94-97`: 「마켓 주문번호 — **아직 저장하지 않는다** … 마이그레이션 + 프로파일 매핑 +
재가져오기다 → 대기목록에 올렸다.」 마켓 관리자 화면과 우리 표를 잇는 **유일한 손잡이**가 없다.
**소급이 안 되는 것이 이 일의 성격이다.**

**정정**: 대기목록에 **없다**(→ 4.12). 적힌 곳은 `docs/작업-로그-2026-08-전반.md:548` 표 한 줄이 전부다.

### 2.8 8만 행을 넣으면 창이 10초 굳고, 진행률도 취소도 없다 — L · ✅

- **메인 스레드**: `import.ts:66` 「위저드 v1은 파이프라인을 **메인 스레드**에서 돌린다
  (ADR-001 조건 2 위반 — 인지된 부채) … 스모크 실측으로 **9.5~10.4초** 화면이 멈춘다.」
  방어는 `BIG_FILE_BYTES = 4MB` 고지 하나(`App.tsx:1095`)
- **진행률**: `run.ts:81`에 `onProgress`가 있고 `:374`가 실제로 부르는데 `src/app/`에 그 문자열 **0건**.
  화면이 아는 것은 `import.ts:328` `vals.impBusy` 불리언 하나다.
  **CLAUDE.md 세션 1 합격 기준이 「30초 이내 (진행 표시 포함)」인데 그 괄호가 미이행이다**
- **취소**: `AbortSignal`/`shouldCancel` 없음. `AbortController` 0건

### 2.9 설치본에는 열 DB가 없다 — 빌드한 기계의 프로젝트 폴더를 연다 — L · ❌

> ★ **C절에서 지금 가장 급하다** (2026-08-20 판단) ★ 다른 항목은 사용자가 무엇을 해야
> 드러나는데, 이건 **아무것도 안 해도 이미 깨져 있다.** 다른 PC에 설치하면 첫 열기에서
> 끝난다 — 배포를 생각하는 순간 이것이 0순위다.

`data.ts:52-57` `declare const __PROJECT_ROOT__` → `DEV_DB_PATH = ${root}/.tmp/pnl.sqlite` →
`:253` `openTauriDriver(DEV_DB_PATH, …)`.

`vite.config.ts:25` 주석은 「스모크 측정 전용 주입(dev에서만 쓴다)」인데 실제로는 **앱의 유일한 DB 경로**다.
`src-tauri/src/db.rs:174` `Connection::open(path)`는 부모 디렉터리를 만들지도, appDataDir을 해석하지도 않는다.
`@tauri-apps/api/path`를 부르는 곳 **0곳**.

**다른 PC에 설치하면 첫 열기에서 실패하고 「이 동작을 마치지 못했습니다」 모달 하나로 끝난다.**

### 2.10 키보드·스크린리더로 앱 대부분을 조작할 수 없다 — L

`Template.tsx` 6,558줄에서 `aria-*`는 `:31`의 `aria-hidden`(장식 svg) **하나**,
`onKeyDown`은 `:6431`(팔레트 입력) **하나**, `role=`·`tabIndex=` **0건**.
`<button` 70개인데 `onClick` 211개 — **141개가 `<span>`/`<div>`/`<tr>` 위**에 있다.
`<input` 32개에 `<label` 7개. Esc를 듣는 곳은 `palette.ts:91` 하나뿐(모달·팝오버는 Esc로 안 닫히고 포커스 트랩 없음).

`docs/핸드오프.md:288-289`가 결함 30·31로 이미 적어 놨고 **어느 § 문서도 이것을 컷으로 선언하지 않았다.**
`Template.tsx`는 `refuseIfPatched`가 지키는 생성물이라 변환기 쪽에서 심거나 §21 패치 영역 선언이 필요하다.

### 2.11 G마켓과 옥션을 나누지 못한다 — M

`channel.ts:215` 「★ G마켓/옥션을 못 나눈다 ★ 그것을 가르는 컬럼은 «판매아이디» 하나뿐인데
ESM 프로파일이 받지 않는다」 → `hasChannels: false` · `channels: []`.
`esm-order@1.json:117`이 열 미수용은 선언했지만 **「G마켓/옥션을 안 나눈다」는 어디에도 제품 결정으로
선언돼 있지 않다.** 「파일에 없다」가 아니라 「프로파일이 안 받는다」인 것이 요점이다.

### 2.13 11번가 정산의 도서산간배송비·반품배송비를 안 받는다 — S

`11st-settlement@1.json:86` 「2026-07 파일에서는 합계 0이다. 도서산간배송비·반품배송비는
**별도 컬럼이며 아직 매핑하지 않았다**.」 — 「열이 없다」가 아니라 **「열은 있는데 안 받았다」**다.
도서산간 주문이 실제로 생기는 달부터 그 배송비가 손익에서 통째로 빠진다.

### 2.14 «1+1»과 단품이 유사도 100% 동점으로 뜬다 (결함 51) — M

`listing-match.ts:60-65` `.replace(/[^0-9a-z가-힣]+/g, " ")` … `.filter((t) => t.length > 1 && !NOISE.has(t))`
— `"1+1"` → `"1 1"` → 길이 1이라 **둘 다 버려진다.** 같은 필터가 `:259`(원가 다리)에도 있다.

`docs/목업-결함-발견분.md:143-155`가 2026-08-14부터 「**상태: 판정 대기**」다. 지금 피해를 막는 것은
`contested` 판정 하나뿐이고, **사용자가 100%를 믿고 합치면 원가가 다른 두 상품이 한 SKU가 된다.**
토큰 규칙을 바꾸면 `0.74 · margin 0.15 · shared 2` 절단점을 실측 분포로 다시 떠야 한다.

---

# C-3. 조항이 선언했는데 코드에 없다

### 3.1 파이프라인 6단계 중 Validation이 적재 시에 안 돈다 — M

`mapping/index.ts:337` `readonly validationRules: readonly Record<string, unknown>[]` — 타입도 있고
프로파일 9장이 값도 채우는데(`esm-order@1.json:96` `required`·`nonNegative`·`parseRate min 0.99`)
**읽는 실행 코드가 0건**이다. `grep 'nonNegative\|parseRate' src/ --include=*.ts` → 0건(JSON 안에만 존재).

같은 병이 둘 더: `:173` `sheetSelector`, `:174` `headerRowHint`도 참조 0건인데 `cost-master@1.json:33-37`이 선언한다.
`import.ts:229`가 스스로 「(그리고 파이프라인의 Validation은 아직 미구현이다)」라 적었다.

**정정**: `validation/`이 빈 것은 아니다 — `identity.ts`를 `analyze.ts:38`·`judge.ts:35`가 쓴다.
`identity.ts:19`가 「**적재 시** 선언 규칙 실행」과 「**판정 시** 관계 증명」은 종류가 다르다고 갈라 놨다.
**스키마는 안 늘고 코드만 늘리면 된다.**

### 3.2 LOCK 5 위반 — 앱 시작에 12개 화면치를 전부 읽고, 표에 절단이 없다 — L

`App.tsx:389-391` `useEffect(() => { void loadDevSnapshot().then(take) }, [take])` — 마운트 즉시.
`data.ts:482` `return loadDevSnapshot(want)`라 **모든 쓰기 뒤에도** 다시 돈다.
`data.ts:322-360`이 한 함수에서 pnl·settlement·order·linking·pendingCost·fieldmap·coverage·history·
product·profit·channel·dailySeries를 전부 await한다.

그중 `core/linking/view.ts:127`은 `WHERE l.library_id = ? ORDER BY l.title` — **기간 필터도 LIMIT도 없다.**
행 표에도 절단이 없다 — settlement/product/history/linking 어디에도 `LIMIT` 0건
(예외 `fileSightings(200)`·`collectionEvents(200)`). 가상 스크롤도 페이지네이션도 없다.

같은 규율을 아는 자리는 따로 있다 — `App.tsx:1301` 「스냅샷에 실어 나르지 않고 **열었을 때만** 읽는다
— batch 하나가 8만 행일 수 있고 … LOCK 5 위반이다.」

### 3.3 §21-2 근거 3깊이 중 L1·L3는 마크업조차 없다 — L

L2는 1.7. L1(「호버 한 문장 + 분모」)을 실을 자리가 없다 — 동적 `title={…}`이 2곳(`:2160`·`:2870`)뿐.
L3(원본 파일·batch·매핑 버전 계보)는 `grep -rniE '\bL3\b|lineage|계보' src/` → 화면과 무관한 관용구만.

### 3.4 §21-5 — derived 값에 층(layer) 속성이 없다 — L

`grep -rn 'ColumnDef' src/ tests/ tools/` → **0건**. 표는 손으로 쓴 JSX + 인라인 스타일이라
fact/derived/judgment를 값에 붙일 구조 자체가 없다.

`dashboard.ts:307` 「품목에 붙는 근거가 없는 칸 — 0이 아니라 «모른다»다 (§22)」 / `fee: "—"` —
**모르는 칸은 정직하게 비웠는데 아는 칸에 근거 진입로가 없다.**
헌장 금지목록 #6이 ColumnDef **엔진**을 보류시키지만 §21-5는 「사상은 지금 지킨다」고 명시 예외를 뒀다.
(※ `costs.ts:401` `vals.layerRows`는 **손익 3층**이지 §21-5의 층이 아니다.)

### 3.5 §21-3 — M 폭의 「지표 칩 + 한 열 + 행 탭 깊이」가 없다 — M

`flowbase-theme.css:114-118` 「표는 가로 스크롤 + 첫 열 고정」. 존치 조항(§9)은 지키지만 §21-3이
요구한 대체물이 아니다. `metricChip|지표 칩|metric-chip` → 0건. 「행 탭 깊이」의 목적지가 L2 패널이라
**1.7을 먼저 잇지 않으면 반쪽이다.**

### 3.6 §21-4 — 신선도의 「소비 지점 표식」이 없다 — M

상태판은 있다(`dashboard.ts:541` · `Template.tsx:1898`). KPI 6장(`dashboard.ts:558-597`)은
`label`·`value`·`sub`·`color` 넷뿐이라 **「이 값에서 빠져 있습니다」 표식 필드가 아예 없다.**
히어로 칩(`:652` `"일부 제외"`)에도 분모가 없다.

### 3.7 §21-1 — 판단층의 「점선 밑줄」이 화면에 한 곳도 없다 — S

`Template.tsx`의 `dashed` 9건이 전부 드롭존 테두리·사분면 보조선·빈 상태 상자다. **값에 붙는 밑줄 0건.**
짝인 표식은 제대로 있다 — `:2166`의 조정 origin 점이 §15-3 규격 그대로다.
**둘 다 붙어야 한다고 확정 블록이 못박았는데 한 짝만 있다.**

### 3.8 §18-A — 「원본이 따로 있다면 그쪽이 안전합니다」 경고가 없다 — S

`grep -rn '안전합니다\|원본 데이터가' src/app/` → **0건**. 있는 것은 (a) 멀티시트면 무조건 뜨는
총론(`:4389`) (b) 시트별 색·수치(`import.ts:432`·`:438`)뿐 — **「고르려 할 때」의 조건부 경고가 아니다.**
`ADR-019:36`이 이 부재를 전제로 우회 가드를 세웠다(가드 본체는 `analyze.ts:382-384`에 실재).

### 3.9 ~~실패가 나면 앱이 아무 말도 안 한다 (3곳)~~ ✅ **닫혔다** (`004fcb6`)

> 브라우저에서 **일부러 앱을 죽여** 확인했다 — 흰 창 대신 「화면을 그리다 멈췄습니다 ·
> 넣어 두신 데이터는 그대로 있습니다 · [다시 열기]」가 뜬다(본문 118자).
> 비동기 사고는 **화면을 안 덮고** 구석 알약으로만 말한다 — 렌더는 멀쩡한데 덮으면
> 멀쩡한 앱을 우리가 죽이는 셈이다.

<details><summary>원문</summary>

- **ErrorBoundary 0**: `main.tsx:57-62`에 경계 컴포넌트 없음. `ErrorBoundary`·`componentDidCatch`·
  `window.onerror`·`unhandledrejection` **0건**. 렌더 예외 하나에 React 19가 트리를 언마운트해
  **빈 흰 창**만 남고, 릴리즈 웹뷰엔 콘솔이 없다
- **마이그레이션 재시도 불가**: `data.ts:277-283` `migrated ??= migrate(db).then(…)` —
  `??=`는 **거절된 프라미스도 non-nullish**라 재할당되지 않는다. `.catch(() => { migrated = null })`가 없고
  모달은 `confirmLabel: "닫기"`뿐. **앱 재시작이 유일한 복구다**
- **미래 스키마 가드 없음**: `migrate.ts:110-120`이 `done.has(m.version)`만 보고 **내가 아는 최대 번호보다
  큰 적용본**을 검사하지 않는다. 옛 빌드가 새 DB를 말없이 열고 쓰기까지 한다

</details>

### 3.10 원가표의 `배송비`를 담을 자리가 없다 — S

`cost-master@1.json:64` 「`배송비`는 원가와 같은 성격이라 `cost_history.kind = 'LOGISTICS'`로 들어갈 수
있고(**지금은 한 파일에서 한 종류만 넣는다**)」.
**정정**: 같은 주석의 `모델명`은 **이미 쓰인다** — `reference.modelColumn` → 011 `model_code` →
`pending-cost.ts:122-124` → `listing-match.ts:334-346` → `linking.ts:316` 「모델 일치」.
열려 있는 것은 **배송비·카테고리·영문 `Model`** 세 열이다.

---

# C-4. 게이트가 거짓 안심을 준다

### 4.1 배선 「100%」가 세 겹으로 거짓 안심을 만든다 — S

실측: 소비 469 · 배선 249 · 컷 220 · **남은 일 0 · 100%** (2026-08-20 밤 기준).

1. **오분류**: `wiring.ts:108`의 정규식이 §21-2 근거 패널(`detail|hasDetail|closeDetail`)을
   라이선스 키·자격증명과 한 묶음에 넣고 `:107` 「미구현 화면 — 설정·라이선스」 · `:109` 「배포 단계 일이다」로
   선언한다. **근거 패널은 설정 화면이 아니고 배포 단계 일도 아니다**
2. **끝이 안 잠긴 정규식**: CUTS 14개 중 `$`를 쓴 것은 `:170` `/^(mixTip)$/` 하나.
   새 이름 11개 중 **10개가 자동으로 컷에 삼켜진다**(`detailPath`·`modalTitle`·`actionsBar`·`stopLoss`·
   `jsonRows`·`restoreAll`·`creds2`·`flagsOff`·`entitiesGraph`·`formulaText`).
   배선된 것 중 13개는 컷 정규식에도 걸려 **배선을 지워도 게이트가 초록이다**
3. **「배선됨」의 정의**: `:45` `/\bvals\.([A-Za-z_$][\w$]*)\s*=[^=]/g` — **값이 비거나 무동작이어도 100%**다.
   행 객체 하위(`manage`·`pick`·`canAck`)는 아예 안 세어진다

`tests/wiring-coverage.test.ts`가 무는 것은 `TODO_MAX = 0`뿐 — **컷 총수가 늘어나는 것을 막는 단언이 없다.**
(부수: `user-invariants.test.ts:227`의 `isDeclaredCut`이 `)$/`로 끝나는 컷을 못 읽어 **13개만** 본다 —
시험과 도구가 세는 컷 집합이 다르다.)

### 4.2 감사 B-3 처방이 코드에 한 줄도 안 들어갔다 — M

`perf-gate.ts:18-19` `TIME_BUDGET_MS = 30_000` / `MEMORY_BUDGET_MB = 256`, `e2e-gate.ts:23-24`가 복제.
**상한 상수가 환경별로 안 갈린다.** 기준선 파일·환경 라벨·상대 판정 0줄.
행 수·시간 분리도 없다 — `e2e-gate.ts:131` `if (timePass && memPass && rowPass)`가 셋을 한 exit code에 묶는다.

실측 분포는 이미 있다: 실기기 205.9~237.2(2026-08-20 밤 6회) · 201.9~254(낮) · 컨테이너 246.9~318.

### 4.3 perf·e2e가 CI에 없다 — M

`.github/workflows/ci.yml:24-65` 전부: `npm ci` · `git diff --exit-code package-lock.json` ·
`npm run typecheck` · `pnl.ts --clean` · `npm test` · `npm run build`.
**환경 무관한 행 수 3중 일치조차 사람이 손으로 불러야 한다.** 4.2를 먼저 풀지 않으면 컨테이너에서 상시 red다.

### 4.4 `npm run convert:gate`는 오늘도 exit 1이다 — M

진짜 판정자는 `tests/convert-gate.test.ts`의 `DEVIATIONS`(:42)·`S21_REGIONS`(:849)인데
**CLI가 닿을 길이 없다**(gate.ts가 import하는 것은 source/parse/ir/jsx-ir뿐).
**「빨간 게이트는 곧 꺼진다」를 훈련시키는 장치가 `package.json:12`에 켜져 있다.**

### 4.5 `cargo test`가 CI에 없다 — Rust는 CI에서 컴파일도 안 된다 — M

`ci.yml:10` `runs-on: ubuntu-latest`, rust 스텝 0. `npm run build`는 vite만 돌아 `src-tauri/`가 깨져도 초록이다.
`db.rs:302-309`가 스스로 적었다: 「TS 쪽 시험은 가짜 `invoke`로 … **그 가짜가 진짜와 어긋나면 둘 다 초록인 채로
앱만 깨진다**」. 그 「진짜」를 재는 `#[test]` 6개가 CI에서 **한 번도 안 돈다**.
※ `db_run_many`·`params_of`의 값 변환은 Rust 시험 0건.

### 4.6 화면 단언이 조용히 skip으로 꺼질 수 있는데 세는 게이트가 없다 — S

`tests/global-setup.ts:36-40`이 실패해도 `console.warn` 후 진행하고, `ready ? describe : describe.skip`
패턴이 **21개 파일**에 걸려 있다. `process.env.CI`를 읽는 코드가 **0곳**.
`DEV_SNAPSHOT` 존재를 단언하는 시험도 없다 — `dev-db-isolation.test.ts:50`은 **조용히 빠져나간다.**
6일간 실제로 통째로 잠든 적이 있고, `6362b3d`는 「실제로 돌았나」를 **사람이 러너 로그를 읽어** 확인했다.

### 4.7 앱 코드 3,000줄에 실행되는 시험이 0건이다 — L

- `App.tsx` 1,686줄 — import하는 테스트 **0개**. 닿는 방식은 전부 문자열이다
- `data.ts` 686줄(DB 열기·쓰기) — import 0개. 대신 `tauri-conn-race.test.ts:92`가 「**모양**」을 복제한다
  — 모양이 본체와 어긋나면 양쪽 다 초록이다. **ADR-014의 WAL 사고가 여기서 났다**
- `migrate-web.ts` — **앱이 실제로 쓰는 로더**인데 시험은 전부 `migrate-node`를 본다.
  `import.meta.glob`은 Vite 전용이라 vitest가 안 탄다. **glob이 파일 하나를 놓쳐도 초록이다**
- `export-wire.ts`(74줄) — 테스트 0건. **LOCK 8이 「전면 개방」이라 부른 기능이다**

`playwright`가 devDependencies에 있는데 쓰는 곳 **0곳** — 브라우저 확인이 매번 손으로 이뤄진다.

### 4.8 equivalence 기준선이 `.gitignore` 안에만 있다 — M

`equivalence.ts:47` `BASELINE = join(OUT_DIR, "equivalence-baseline.json")`(OUT_DIR = `.tmp`).
clone 직후 `--check`는 「기준선이 없다」로 끝나고, 그 자리에서 `--save`를 하면
**지금 코드가 자기 정답지가 된다** — 감사 B-1이 매니페스트에서 끊은 고리가 여기 그대로 남아 있다.

### 4.9 CI는 ubuntu 한 대인데 제품은 Windows 데스크톱 앱이다 — M

matrix 없음. `tests/dev-db.ts:103` `PRAGMA journal_mode = DELETE`처럼 파일 잠금 의미론에 기대는 자리가 있고,
ADR-014의 사고도 실기기에서 났다.

### 4.10 U-7·U-8은 「집행자」 파일 안에 단언이 0개다 — S

`docs/사용자-불변식.md:3` 「이 문서의 모든 줄은 `tests/user-invariants.test.ts`가 지킨다」 →
`grep -rn "U-7\|U-8" tests/` = **0건**.
※ 완화: `:122-123`의 집행 표는 위임을 정직하게 밝힌다. **거짓인 것은 머리말이다.**
위임받은 `tests/scope-bar.test.ts:85`는 U-8 이름 없이 `(ADR-021)`로만 붙어 있어 이름이 바뀌면 아무것도 신고하지 않는다.

### 4.11 래칫 둘이 실제보다 작게 센다 — S

- `user-invariants.test.ts:385` `/click:\s*\(\)\s*=>\s*\{\s*\}/` — **이름 하나만** 센다.
  실사용 행 객체의 무동작 핸들러는 **최소 14개**다
- `WARN_MAX = 3`(`:92`)이 `data.ts` 한 파일만 세고 실측도 3 — **여유 0**. `App.tsx:335`는 게이트 밖
- `UNDECLARED_MAX = 0`(`:247`)은 통과하지만 시험 자신이 적었다: 「선언된 것도 화면에 버튼이 그려져 있으면
  사용자는 여전히 고장으로 읽는다」

### 4.12 대기목록 문서에 등재가 빠졌다 — S

코드가 「대기목록에 올렸다」고 적은 **마켓 주문번호**(`order.ts:97`)가 목록에 없다.
※ 「기준 가져오기의 기록 화면 표출」은 **2026-08-20에 항목 8로 등재됐다.**

---

# D. 의도적 컷 — 지금 안 하기로 정한 것

기계적 증거가 있는 것만 넣었다 — `wiring.ts:59-186`의 `CUTS`, ADR의 「범위 밖」, 코드 주석의 명시 선언.
**분류 주의**: `CUTS`는 최상위 `vals.*`만 세므로 행 객체 하위(`canAck`·`hasChannels`·`price`)는 못 오른다.

| 무엇 | 근거 | 크기 |
|---|---|---|
| 설정·라이선스·테마 저장 계층 (localStorage **0건**, 켤 때마다 dark 복귀) | `wiring.ts:110` · `shell.ts:112` | L |
| 저장된 뷰 (무료 티어 「2개」 게이팅의 근거가 0) | `wiring.ts:74` · `vals.ts:1342` | L |
| URL 소스 기억 | `import.ts:315-318` · ADR-013 | L |
| 광고 배분 룰 표 | 014가 성격을 바꿨다 — **재설계 선행** | L |
| 진단 화면 (§22-4 표면 4곳 중 유일한 미이행) | `shell.ts:270` · `Template.tsx:2492` | M |
| 진단 할 일 채택/기각 기록 | 012의 PK가 `(library_id, notice_key)`라 **결정·사유 칸이 없다** | L |
| §20 확인 질문 · 드리프트 다이제스트 (타입·마크업만) | `wiring.ts:124` · `fieldmap.ts:13` | L |
| §21-4 워터폴 | `dashboard.ts:555` · 8월 파일 적재 선행 | M |
| 정산 대사 「확인함으로」(`recon_ack`) | ADR-020 「범위 밖」 · `settlement.ts:213` | L |
| 판매가·마진율 «—» | `linking.ts:204` 「없는 것을 지어내지 않는다」 | M |
| 주문 단위 기여이익 배분 | `order.ts:98` 「임의 배분을 넣으면 **계산기가 둘이 된다**」 | L |
| `fact_settlement`에 마켓 조정금액 컬럼 | ADR-020 범위 밖 ②. ※ ADR이 적은 「012」는 낡았다 — **015**다 | L |
| **연결 정체성 `conn-${marketplaceKey}`** ★위험 | `App.tsx:1169`. fact 5종이 `UNIQUE (connection_id, source_key)`라 **같은 마켓 계정 둘이면 한쪽이 다른 쪽을 덮는다.** ADR-021:112 「**이것도 급하다 (별건)**」 | L |
| 판정 4단·항등식을 `file_column`에 저장 | ADR-017 「v1의 판정은 주석일 뿐」 | L |
| §22-5 grain 원칙 | §22:369 「11번가 주문 파일이 도착하면 **실측이 먼저다**」 | M |
| 자사몰 옵션 추가금 과소계상 (0.03%) | `selfshop-order@1.json:108` — 파일에 열이 없다 | S |
| 표 필터 · 열 숨김 · 표시 설정 · 상태 칩 | `wiring.ts:157`·`:150` · **LOCK 10(상태 칩은 배선하면 위반)** | M |
| 원본 픽스처 대조가 CI 밖 | 헌장 E-10. **안 된 것은 「실기기 전용」이라 적고 실행을 기록하는 것** | M |
| 설정 화면의 macOS 경로 하드코딩 | `ssData: false`라 렌더 안 됨 | S |

> ## ★ 이 중 **마크업 제거만 남은 것 4건** ★
>
> 컷 사유는 성립하는데 「배선하지 마라」까지만 말하고 **「그리지도 마라」를 안 말해서** 죽은 컨트롤이 남았다.
> **§21-1/U-3 이행은 배선이 아니라 제거다.**
>
> - 필터 아이콘 `Template.tsx:381` (가드 없음 = 전 화면)
> - 표시 설정 아이콘 `:430`
> - 내보내기 「숨긴 컬럼도 포함」 체크박스 4곳 `:2026·2338·3106·3790`
>   ★ **2026-08-20에 팝오버를 살려서 이제 눈앞에 뜬다**
> - 상태 칩 `:337`
>
> ※ 아이콘·상태 칩은 `flowbase-theme.css:181-182`가 **768px 미만에서 숨긴다** — 「전 화면」은 768px 이상 한정.
>
> ※ **필터는 A-3(묶음 자격)과 겹친다.** 채널·카테고리 필터가 「무엇을 합쳐 볼 것인가」에
> 묶음과 다른 답을 낼 수 있다. **ADR-023 확정 전에 배선하지 않는다.**

---

# E. 이미 닫혔는데 문서·주석이 안 따라온 것

코드는 맞는데 주석·문서가 틀렸다. **다음 사람이 잘못된 지도를 받는 자리다.**

| # | 어디 | 무엇이 틀렸나 |
|---|---|---|
| 1 | `costs.ts:252`·`:264` | 「운영비 표는 **읽기 전용**이다 … 이번 세션에서 자른 범위」 → **거짓.** `:329`가 제어부를 열고 `App.tsx:857-903`이 9개를 전부 배선했다 |
| 2 | `driver-wasm.ts:24` | 「웹판은 **보기 전용**이고 화면이 그 사실을 말한다(`isWasm`)」 → **두 겹으로 낡았다.** `isWasm`은 이 주석 줄 하나뿐(정의·사용처 0). 실제 심볼은 `data.ts:209` `isWebDemo()`. 「보기 전용」은 ADR-018이 명시적으로 번복했다 |
| 3 | `order.ts:101` | 「`fact_order_item`이 **비어 있다**」 → 거짓 (→ 1.11) |
| 4 | `sign.ts:31` · 대기목록:93 | 「이 함수가 그 자리를 **대신한다**」 → 호출부 0곳 (→ 1.8) |
| 5 | `cost-master@1.json:64` | 「`모델명`은 … **아직 자리가 없다**」 → 모델 매칭은 **이미 돈다** (→ 3.10) |
| 6 | `order.ts:97` | 「→ **대기목록에 올렸다**」 → 없다 (→ 4.12) |
| 7 | `작업-상태.md:397-399` | 「`signPolicy`의 **집행자가 없다**」 → `mapping/index.ts:961`이 집행한다 |
| 8 | `ADR-020:184` | 「범위 밖 ②(**마이그레이션 012**)」 → 012는 `notice_ack`. 실제 번호는 **015** |
| 9 | `wiring.ts:104-106` | 「`openCmd`처럼 **목업에만 있고 우리가 안 쓰는 이름**」 → `Template.tsx:88`이 실제로 쓴다. ★ **2026-08-20에 배선됐다** |
| 10 | `wiring.ts:182-183` | 「표시용 상수 … **값을 넣어도 화면이 달라지지 않는다**」 → `stop`(팔레트가 닫힌다)·`firstRunLabel`(빈 버튼이 뜬다)·`heroBasisGo`에서 거짓 |
| 11 | `사용자-불변식.md:3` | 「이 문서의 **모든 줄은** … 지킨다」 → U-7·U-8 단언 0건 (→ 4.10) |
| 12 | `palette.ts:18-20` | 「아직 안 만든 화면은 목록에 넣지 않는다」 → `UNBUILT`가 diag 하나뿐 (→ 1.4) |

---

# 2026-08-20 밤에 닫힌 것

| 무엇 | 커밋 |
|---|---|
| 원가 결과 화면의 거짓 문구 셋 + 대기실 건수 | `dfbe88b` |
| 팝오버 16곳(`stop`·`stopEvt`) + 사이드바 ⌘K 상자(`openCmd`) | `8558cd0` |
| `npm run state` · `/before-work` · `/after-work` · 대기목록 8 | `37bc49a` |
| ADR-023 · 인계 문서 실측 갱신 | `6c76e72` |

이 문서를 소비하면 **지운다.** 항목이 닫힐 때마다 여기서 지우고, 남은 것이 0이 되면 파일을 지운다.
