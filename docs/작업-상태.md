# 작업 상태 — 다음 세션이 여기서 이어받는다

마지막 갱신: 2026-08-21 (**★ 세션 4부 — 커밋 8개 · 통(015·016) · ADR 023·026·027 · 방향 게이트 ★**) · `main`

> ✅ **날짜 정본 = `git log`의 커밋 시각** (2026-08-20 확정)
>
> 한동안 문서 13자리가 `2026-08-21`이었는데 **커밋 이력에 08-21은 한 건도 없었다.**
> 「확인되지 않았다」로 두면 매 세션이 같은 판단을 다시 하므로 규칙으로 닫는다 —
> **문서의 날짜는 커밋 시각을 따른다.** 13자리를 08-20으로 정정했다.

```
게이트 (2026-08-21 · 이 기기 = win32 · **node v24.13.0** — 08-20 세션은 v22.14.0이었다)
           typecheck ✅ · npm test **1,309 passed / 1 skipped (96파일)** ✅ · build ✅ 9.6초
           convert-gate 12건 ✅ · wiring 합계 100%
           <!-- facts:wiring -->배선 258 · 컷 211 · 남은 일 0 (소비 469)<!-- /facts -->   ← `npm run facts -- --write`가 적는다
           ★ gates:selftest **12/12** — 등재된 흠집을 넣고 가드가 붉어지는지 본다 (신설)
           ★ perf · e2e 는 **기기 사실이라 여기 숫자를 안 적는다** — `npm run perf` · `npm run e2e`로 잰다
             (2026-08-20 실기기: perf 2.7초 · e2e 5.6~5.8초 · +205~237MB · 행 수 3중 일치 80,137.
              같은 코드가 컨테이너에서 246.9~318MB를 낸다 — 절대 상한이 환경을 재는 문제는 B-3에 열려 있다)
           <!-- facts:schema -->스키마 정본 17<!-- /facts -->   ·   실 DB가 여기 따라왔는지는 `npm run state`
           convert:gate CLI는 **빨간 것이 정상** (아래 절 참조)

이전 값 (클라우드 컨테이너 · 2026-08-20 밤)
           npm test 1151 · build 9.2초 · 배선 246 / 컷 223   ← 그 시점의 값. 지금 값은 위 마커가 적는다
           ⚠ 그 세션이 적은 「실기기 DB 13」·「리스팅 190건 미연결」은 **둘 다 틀렸다**
             — 실측 11 · linked 236 / 미연결 0. 그래서 `npm run state`가 생겼다
```

> **숫자를 인용하기 전에 `npm run state`를 돌린다.** 위 블록도 그 출력으로 맞춘 것이다.
> 기기가 다르면 숫자도 다르다 — 이 문서가 그걸로 두 번 틀렸다.


⚠ **B-3은 그대로 열려 있다 — 다만 오늘 실기기 분포가 하나 더 쌓였다.**

2026-08-20 밤 실기기 6회(perf 3 · e2e 3)는 **205.9~237.2MB**로 전 회차 통과했고
회차 간 편차도 좁았다(perf 15MB · e2e 21.8MB). 시간은 2.7초 / 5.6~5.8초로 상한
30초에서 멀다. 그래도 **판정 방식의 문제는 안 없어졌다** — 컨테이너에서 같은
코드가 246.9~318MB를 낸다. 절대 상한 256MB가 두 환경 사이를 지나므로 여전히
「코드」가 아니라 「환경」을 재고 있다.

처방은 그대로다: **① 행 수·시간은 CI로 분리**(환경 무관) **② 메모리는 환경별
기준선 대비 상대 판정.** → [문서 감사 B-3](문서-감사-2026-08-20.md)

**시간과 행 수 3중 일치는 환경 의존이 없고 세 회차 전부 통과했다** — 적재의
정확성은 확인됐다. 👉 처방은 둘: **① 행 수·시간을 CI로 분리**(환경 무관이라 지금
가능) **② 메모리는 절대 상한을 버리고 환경별 기준선 대비 상대 판정으로.**
→ [문서 감사 B-3](문서-감사-2026-08-20.md)

---

---

## ★★ 다음 손 — 여기 하나뿐이다 ★★

> 이 절이 **유일한 「다음 손」**이다. 이전에는 10곳에 흩어져 각자 다른 「다음」을
> 가리켰다 (2026-08-20 감사). 새 「다음 손」을 이 문서 다른 곳에 쓰지 않는다.

### ✅ 0순위·1순위는 **닫혔다** (2026-08-20 저녁)

감사 A절이 「앱이 지금 거짓말한다」로 올린 것과 「고장으로 읽히는 자리」가 전부
코드로 닫혔다. 무엇을 어떻게 닫았는지는 아래 「이 세션」 표에 있다.

```
A-1-1 직접 광고비 0 리터럴     ✅ 014 · ADR-022 — 광고비의 95.3%가 상품까지 내려간다
A-1-2 할인 3단 죽음           ✅ 등록부 + 전사 손익. 두 화면이 다른 답을 내던 것까지
A-2-1 헤더 「가져오기」         ✅ · A-2-2 내보내기 5종 ✅ · A-2-3 ⌘K ✅
A-3   768px 기간 선택기 소실   ✅ 목업 결함 68 — 탈출구가 자기를 지우고 있었다
A-4-1 원가 저장 침묵          ✅ · A-4-2 잠금 침묵 11자리 ✅
```

### ★★ 다음 손 — 둘이 나란히 열려 있다 ★★

> **① 설계 쪽** — [ADR-023](ADR-023-통-묶음-자격-입구순서.md)의 확인 질문 4개에 답을 받는다 (아래)
> **② 코드 쪽** — [코드-남은일](코드-남은일-2026-08-20.md)의 남은 순서 (바로 아래 표)

#### 코드 쪽 — 실행 순서 (2026-08-21 세션이 16번까지 닫았다)

```
✅ 0~7  지도 저장 · KST · 파서 경고 · 주문 품목 · 드롭존 · 정산 항등식 · 데모 컨트롤 · 빈 흰 창
✅ 8   2.9  설치본 DB 경로               3a3daea · ADR-024
✅ 9   2.6  광고비 탭                   2a06539 · ★ 미루기 사유가 실측에서 무너졌다(아래)
✅ 10  1.8  음수 금액 무방비             aa67986 · ADR-025 · ★ 「전부 양수」가 먼저 틀렸다
✅ 11  1.7  ③ 숫자 → 행 드릴다운          §21-2 L2 근거 패널. **다섯 표가 전부 내려간다**
✅ 12  2.14 «1+1» 동점            ADR-026 · ★ 「막고 있다」던 문서가 거짓이었다
✅ 13  2.13 안 받은 컬럼의 돈       ★ 「매핑을 더 한다」가 아니라 「말한다」였다 (아래)
✅ 14  ①   **통 얕은 층 — 마이그레이션 015** · ★ 장부는 이미 있었다(009). 승격시켰다
✅ 15  ①   **화면 — 「가져오기 기록」이 통을 읽는다.** 원가 파일이 화면에 떴다
✅ —   가드  **의존 방향 게이트** — core→packs·core→app·packs→app import 0건을 못박았다
✅ 16  ①   **통 깊은 층 (016 · ADR-027)** — 재보니 **gzip 블록이 42배 작았다**
✅ 17  ①   **기준 경로에도 표를 담는다** — 원가 파일이 0 → **1블록 · 253행**
✅ 18  ①   **표 뷰어 + 「출처」 칸 (ADR-028 · §21-8)** — 원가 파일을 눌러 원본 표를 연다
✅ 19  ②   **묶음 자격 (017 · ADR-023 확인 ①②)** — ★ 절단점은 «만들지 마라»가 아니라
           **«이미 있는 것을 걷어내라»**였다. `costed === skus`가 100%짜리였다
✅ 20  ③   **입구 순서 (ADR-023 결정 4 · 확인 ⑤⑥)** — (a) 대시보드 게이트 · (b) 사이드바
           ★ 「NAV 배열 한 줄」이 **틀렸다** — 나열은 동결 목업 마크업이 한다
▶  21  C절 잔여  ← 다음 세션 시작점
       세부: 2.10 접근성(L) · 2.11 G마켓/옥션 분리(M) · 1.6 나머지 · C-3 · C-4 ·
           3.3 L1·L3 · **3.10 원가표의 «배송비»를 담을 자리가 없다** ← 018이 화면에 띄웠다
```

### ✅ 19번은 닫혔다 — 묶음 자격 (2026-08-21)

**설계는 이미 닫혀 있다** — [ADR-023](ADR-023-통-묶음-자격-입구순서.md) 결정 2 + 확인 ①②.
§22의 `requires[docType]` 판정을 **연결이 아니라 묶음 위에서** 돌린다.

> ★ **확인 ②가 이 일의 전부다** ★ 「열되 부족분을 말한다」이지 통과/탈락이 아니다.
> **비율 절단점을 만들지 않는다** — 아래가 나오면 확인을 어긴 것이다:
>
> ```
> if (ratio < 0.9) return locked      ← 이걸 쓰면 안 된다
> ```
>
> 묶음은 «자격을 판정받는» 것이 아니라 **«무엇이 갖춰졌고 무엇이 비었나»를 선언**한다.
> 결정 2의 이름은 「자격」이지만 실제 기계는 **게이트가 아니라 명세**다.
> 잠기는 것은 오직 «서류가 아예 없을 때»다 — `ad` 파일이 하나도 없으면 ROAS는
> 열리지 않는다. 그건 부족이 아니라 부재이고 §22-1이 이미 그렇게 적었다.

**재는 법** — 원가가 236/261인 묶음에서 상품별 손익이 **열리고**, 그 화면이
「원가 미입력 25건 — 이익이 실제보다 큽니다」를 그 자리에서 말한다.

#### 무엇이 됐나 — 실측 (2026-08-21 · 이 기기 linux · node v22.22.2 · `npm test`)

```
마이그레이션 017   active_batch 뷰 — 커버리지의 «보유» 판정이 묶음을 본다
core/coverage     3상태로 센다: 부재(0) · 부분(0<n<need) · 완전(n=need)
                  CoverageEntry.partial · Coverage.hasPartial · scopeCoverage()
채널 화면          스트립이 partial로 노란 점을 정한다 + 3절 문장에 ②′가 생겼다
시험              1272 → 1280 (+8) · 0 실패 · 부러뜨려 확인함(아래)
```

> ★★ **19번의 뜻이 실측에서 바뀌었다** ★★
> 「비율 절단점을 **만들지 마라**」로 읽고 들어갔는데, 코드에는 **이미 있었다** —
> `src/core/coverage/load.ts`의 `cost.n === cost.skus`. 90%가 안 되는 것과 100%를
> 요구하는 것은 같은 물건이다. 둘 다 236/261을 «없음»으로 접는다.

> ★ **같은 사실에 두 층이 다른 답을 내고 있었다** ★ (ADR-023 따름 조건 5가 금지한 모양)
> ```
> 커버리지  load.ts        236/261 → 「매입원가 잠김」 · 「기여이익 잠김」
> 손익      gaps.ts        cogs-missing 「일부 미입력」 → 열고 «이익이 부풀어 있다»
> ```
> **손익 쪽이 이미 확인 ②대로였다.** 그래서 이건 새 규칙이 아니라 뒤처진 한 층을
> 나머지에 맞춘 것이다.

> ★ **번복을 양쪽에 적었다** ★ §22-3 ①「원가는 «전부»여야 열린다」가 뒤집혔다.
> [ADR-023](ADR-023-통-묶음-자격-입구순서.md)의 「뒤집는 것 하나」 절과
> [§22-3 ①](§22-커버리지-부재의-일급-취급.md) **양쪽에** 적혀 있다 — 사슬 1.

> ★ **§22가 자기 옛 결정을 이미 반증하고 있었다** ★ 「부재는 boolean이 아니다」 각주가
> «부재를 참/거짓으로 접는 순간 일급이 아니다»라고 적었는데, `«전부»여야 열린다`가
> 정확히 그 접기였다. 새 결정이 §22의 더 근본 규율로 돌아온 것이다.

> ★ **부러뜨려 확인했다** ★ ① `cost.n > 0`을 `cost.n === cost.skus`로 되돌리고
> ② `FROM active_batch`를 `FROM batch`로 되돌려 돌리니 새 시험 둘이 붉어졌다
> (「범위 밖 파일은 보유가 아니다」 · 「원가가 반쪽이면 열리고 부족분을 말한다」).
> 절단점 금지는 **비율을 훑어서** 지킨다 — 1/1000부터 999/1000까지 전부 열리는지 본다.
> 「지금 그런 줄이 없다」는 오늘의 사실일 뿐이라 내일 다시 생긴다.

> ★ **방향을 문장에 박았다** ★ 한 부족이 두 지표를 **반대 방향**으로 틀리게 한다 —
> 매입원가는 «덜», 기여이익은 «크게». 지표 이름을 기계적으로 끼우면
> 「매입원가가 실제보다 큽니다」가 나온다. 그래서 문구가 방향을 들고 있다.

#### ✅ 20 (a) 대시보드 게이트도 닫혔다 — 브라우저 실측 (2026-08-21)

`scopeCoverage()`가 화면에 섰다. 「담지 못한 것」 카드의 **맨 앞 줄**이다.
새 화면을 안 팠다 — ADR-023 확인 ④(「기존 화면에 얹는다」) 그대로.

```
재는 법:  데모 DB 사본에 정산 파일을 뺀 묶음을 만들고
          npx tsx tools/harness/render-screen.ts dash <그 DB>

  묶음 있음   보는 범위: 7월 주문만 · 묶음 1개
              이 숫자가 담지 못한 것 → **묶음 「7월 주문만」 · 수수료 · 상품 기여이익 잠김**
  전체        보는 범위: 전체 · 묶음 0개
              범위 줄 **없음** — 합집합으로는 전부 열린다. 할 말이 없으면 안 한다
```

> ★★ **이 줄이 연결별 줄과 «다른 답»을 낸다 — 그게 존재 이유다** ★★
> 데모 DB 실측: 11번가 `[settlement]` · ESM `[order,cost]` · 자사몰 `[order]` —
> **상품 기여이익이 연결 셋 전부에서 잠긴다.** 그런데 **범위로는 열린다**(합집합).
> 어느 채널도 혼자서는 못 여는 지표를 범위가 연다. 판정이 범위로 올라가는 이유다.

> ★ **원가 부족분은 이 줄이 말하지 않는다** ★ 바로 위 `pnlGaps`의 `cogs-missing`이
> 이미 말하고 **모집단이 다르다** — 이쪽은 연결에 붙은 SKU 261개, 저쪽은 그 기간에
> 실제로 팔린 품목이다. 나란히 놓으면 한 카드가 같은 일을 두 숫자로 말한다
> (§22 「알려진 겹침」). 기간 손익이 얼마나 부풀었나는 저쪽이 더 정확하다.

> ★ **광고비는 이 줄에 안 나온다** ★ §22-3 ② — 광고를 하지 않는 셀러에게
> 「광고 파일이 없습니다」는 잔소리다. 잠긴 지표는 조용하다.

#### ✅ 20 (b) 사이드바도 뒤집혔다 — 렌더 실측 (2026-08-21)

```
재는 법:  npx tsx tools/harness/render-screen.ts dash public/demo.sqlite
          → .tmp/dash.html 의 사이드바

  1. 연동   2. 가져오기   3. 가져오기 기록   4. 채널
  5. 현황   6. 대시보드   …
```

> ★★ **「NAV 배열 한 줄이면 된다」가 실측에서 틀렸다** ★★
> ```
> NAV 배열이 정하는 것   ⌘K 팔레트 순서(palette.ts) · 순회용 키 목록
> 사이드바 나열 순서     src/app/generated/Template.tsx 하드코딩
>                       ← mockup/FlowBase.dc.html(동결본)의 기계 변환 결과
> ```
> `NAV`만 뒤집고 렌더하니 **사이드바가 하나도 안 움직였다.** 둘 다 바꿔야 한 방향을 본다.

> ★ **시험이 하나도 안 물었다 — 그래서 게이트를 세웠다** ★ `palette`·`shell`·
> `period-picker`가 `NAV`를 **순회만** 해서, 순서를 뒤집어도 아무 게이트가 말을 안 했다.
> `tests/shell.test.ts`에 앞 셋(`import`·`sync`·`dash`)과 나머지 상대 순서를 박았다.

> ★ **요소를 더하지도 빼지도 않았다 — 블록 이동이다** ★ 「연동」 그룹이 이미
> «넣는 자리»(채널·가져오기·기록)였다. 그 블록을 통째로 올리고 그룹 안에서
> `가져오기`를 첫 항목으로 뒀다. 이탈은 `DEVIATIONS`의 `from`/`to` **네 짝**
> (`texts`·`holes`·`classes`·`attrs`)으로 한 자리도 안 흘리고 적힌다.

> ★ **머리글 여백은 이탈이 아니라 «따른» 것이다** ★ 목업 규칙이 「첫 그룹만 6px,
> 나머지 12px」다. 옮긴 뒤 그대로 적용하니 `styles` 시퀀스가 **원본과 완전히 같아졌다** —
> 선언 0건.

> ★ **빈 앱은 「가져오기」로 연다** (확인 ⑤) ★ `landingView()` — 넣은 파일이 0이면
> 대시보드가 아니라 가져오기로 떨어진다. **데이터가 있으면 안 건드린다** — 매일 쓰는
> 사람을 매번 입구로 보내면 입구를 고친 게 아니라 다른 자리를 망가뜨린 것이다.
> 읽기 **실패**는 «없다»가 아니라서 따로 가른다(§22). 순수 함수로 뺀 이유는 시험을
> 걸기 위해서다 — `setState` 콜백 안에 두면 React 없이는 못 잰다.

#### (참고) 옛 계획 — 확인 전 메모

ADR-023 결정 4의 나머지 절반이다:

```
지금    NAV = [dash, settlement, products, diag, costs, orders, connect, import, sync, …]
        ↑ 출력이 입구에 있다 (src/app/shell.ts)
앞으로  ① 가져오기 → ② 기록(통) → ③ 묶음 → ④ 대시보드
```

**기술적으로는 배열 한 줄이다.** 게이트도 순서를 안 박아 놨다 — `palette`·`shell`·
`period-picker` 시험이 `NAV`를 **순회만** 한다. 그런데 **앱을 켤 때 처음 보이는 화면이
바뀌므로** 손대기 전에 확인을 받는다.

> ⚠ **결정 4는 08-21 확인 질문 4개에 없었다.** ①②는 판정 단위와 기준선, ③은 보관
> 깊이, ④는 「새 화면을 안 판다」였다. **사이드바 순서 자체는 확인받은 적이 없다** —
> 2026-08-21에 사용자가 「(a)만 — 게이트부터」를 골랐고, (b)는 눈으로 보고 정하기로 했다.
> 손대기 전에 확인 질문을 만들어 ADR에 박는다 (「ADR 링크가 없으면 결정이 아니다」).

---

#### ✅ 18번은 닫혔다 — 브라우저 실측 (2026-08-21 · 데모 DB)

```
장부 머리글   시각 · 출처 · 유형 · 대상 · 조회 · 신규 · 갱신 · 실패 · 결과 · 소요
                     ↑ 「연결」이었다 (ADR-028 · §21-8 · DEVIATIONS texts 1건)

첫 줄        2026-01 통합 매출 대시보드.xlsx │ 기준 데이터 │ 원가 대기 171 · … · SKU 신규 35
             ↑ 「채널 없음」이었다. 이제 어느 파일인지 보인다

그 줄을 누르면
  제목        「2026-01 통합 매출 대시보드.xlsx — 원본 표」
  열          상품번호 · 상품명 · 모델명 · 원가 · 배송비 · 카테고리 · Model   ← 파일의 머리글 그대로
  첫 행       4610722841 │ 머레이 트윈 …TWIN-200 │ TWIN-200 │ 14500 │ 2000 │ 계절상품_겨울 │ TWIN-200
  꼬리        253행 중 1–50

회귀 확인    Fact 줄(11번가)은 그대로 「정산 — 넣은 결과」 · 마켓 번호·주문 번호… · 128행 중 1–50
```

> ★ **17번의 「두 번째 호출은 필요 없다」가 18번에서 뒤집혔다** ★
> 17번에는 참이었다 — 열 목록·batch·헤더 행 셋 다 없어서 다시 부를 이유가 없었다.
> **표 뷰어가 열 이름을 요구하면서 하나가 생겼다:** 담긴 표는 데이터 행뿐이고
> 머리글은 `getSummary()`에만 있다. 안 실으면 화면이 「1열 · 2열 …」을 그린다.
>
> 실측이 그것을 먼저 보였다 — Fact 3파일은 `file_column` 59·15·93행인데
> **기준 데이터만 0행**이었다. 이제 7행이다.
>
> **그래서 인계가 경고한 「2번 봤다」 함정이 이제 실재한다.** 17번이 세운 시험
> (「파일 하나를 넣으면 `seen_count`가 1이다」)이 정확히 그 자리를 지킨다 —
> 그 시험이 없었으면 이 커밋이 조용히 그것을 깨뜨렸다.

> ★ **원본 표는 «바꾸지 않고» 낸다** ★ 천단위 쉼표도 안 넣는다(`14500`이지 `14,500`이
> 아니다). 열 이름도 번역하지 않는다 — 파일과 다른 글자가 보이면 그건 원본이 아니다.
> 헌장 C-4가 막는 것은 **내부 코드값**이지 파일이 쓴 머리글이 아니다.

> ★ **1.6이 한 자리 줄었다** ★ 기준 데이터 행은 `cursor: pointer`를 달고도 아무 일이
> 없었다(§21-1 U-3 위반). 018이 그 자리를 채웠다.

> ★ **018이 3.10을 눈앞에 띄웠다** ★ 원본 표의 열에 **「배송비」가 있다.** 원가표가
> 배송비를 들고 오는데 담을 자리가 없다는 것이 이제 화면에서 보인다 — 남은일 3.10.

---

#### ✅ 17번은 닫혔다 — 실측 (2026-08-21 · 이 기기 win32 · node v24.13.0)

```
재는 법:  npm run demo-db  뒤에
          SELECT s.source_name, COUNT(b.sighting_id), SUM(b.row_count)
            FROM file_sighting s LEFT JOIN sheet_block b ON b.sighting_id = s.id
           GROUP BY s.id;

  2026-01 통합 매출 대시보드.xlsx   기준   1블록 ·   253행 ·   9.9KB   ← 0이었다
  2026년 7월_11st 결제일 정산확정.xls  Fact  1블록 ·   128행 ·  11.1KB
  26년 7월 ESM 주문통합검색.xlsx      Fact  1블록 ·   160행 ·  10.9KB
  B2B_매출정리 예시 파일.xlsx         Fact  1블록 ·  1197행 · 197.7KB
```

> ★ **처방이 절반만 맞았다 — 두 번째 호출은 필요가 없었다** ★
>
> 인계가 「목격을 앞으로 당기고 **뒤쪽 호출에 `countAsSeen: false`**를 준다」로 적었다.
> 앞 절반은 맞았다(LOCK 5 — 전 행을 모아 두면 그게 전체 적재다). **뒤 절반은 아니었다.**
>
> `run.ts`가 두 번 부르는 이유는 **열 목록과 batch id가 루프 뒤에야 손에 들어오기**
> 때문이다. 기준 경로는 셋 다(열 목록 · batch · 헤더 행) 처음부터 끝까지 **없다** —
> `columns: []` · `batchId: null` · `headerRowIndex: null`. 두 번째 호출이 쓸 것이
> 하나도 없다. 그래서 **안 불렀고, 그러니 막을 함정도 없다.** 「2번 봤다」를 막는
> 가장 확실한 방법은 플래그가 아니라 호출을 없애는 것이었다.
>
> 시험으로 못박았다 — 「파일 하나를 넣으면 `seen_count`가 **1**이다」. 누가 두 번째
> 호출을 되살리는 날 이 줄이 붉어진다.

> ★ **담기를 «열 찾기보다 앞»에 뒀다 — `run.ts`와 다른 자리다** ★
> 기준 경로는 열(`상품번호`·`원가`)을 못 찾으면 시트가 통째로 실패한다. 그런데
> **사용자가 표를 가장 보고 싶은 순간이 정확히 그때다**(「무슨 열이 있길래 못 찾나」).
> 담기를 뒤에 두면 실패한 시트만 못 연다 — 제일 필요한 것을 못 주는 배치다.

> ★ **표 보관 실패는 `failed`가 아니다** ★ 적재는 성공했는데 시트 실패로 부르면
> 사용자는 **원가가 안 들어간 줄** 안다. 새 실패 상태를 발명하지 않고(LOCK 6)
> 015가 이미 그리는 `sighting_outcome`에 `sheet_store_failed`로 편입했다 —
> 라벨은 `src/app/history.ts`가 소유한다(「표 보관 실패」 · 헌장 C-4).

> ★ **부러뜨려 확인했다** ★ 담기를 끄고 돌리니 새 시험이
> 「원가 파일이 0블록이다 — 장부에는 섰는데 표로 못 연다: expected 0 to be greater
> than 0」으로 붉어졌다. 「블록이 생겼다」만 재면 **빈 블록도 통과**하므로, 꺼낸 첫 행을
> **파서를 다시 돌린 결과**와 맞춘다.

> **17 다음은 18(표 뷰어)이다.** 둘을 붙여서 하면 「원가 파일을 눌러 표를 본다」가
> 한 번에 서고, 그게 ADR-023이 그린 그림이다. 18은 **눈에 보이는 것**이라
> 「다음 손」 규칙(④)에도 맞는다.

> ★ **16번 — 보관 비용을 재서 형태를 골랐다 (ADR-027)** ★
> 픽스처 #13(원본 10.05MB · 80,138행 × 43열)을 실제 SQLite에 넣고 쟀다:
> 평문 TSV **43.0MB**(4.3배) · 셀당 한 행 **24.2바이트/셀**(→80MB+ · 344만 «행») ·
> **gzip 블록 2,000행 1.02MB(42배 작다)**. 실 DB가 34MB인데 평문이면 광고 파일
> 하나로 두 배를 넘긴다.
>
> **JSONL을 골랐다 — 무손실에 5%를 낸다.** gzip TSV 1.02MB vs gzip JSONL 1.08MB인데
> TSV는 탭·개행이 든 셀을 망가뜨린다. ★ **오늘 픽스처에 그런 셀이 0개라 TSV를 골라도
> 아무 시험이 안 깨진다** ★ — 그래서 합성으로 판을 깔았다(`sheet-block.test.ts`).
>
> BLOB이 아니라 **base64 TEXT**다 — 드라이버 계약(`string | number | null`)을 380KB
> 때문에 넓히지 않는다. 그래도 평문의 1/29이다.
>
> ✅ **원가 파일도 담겼다 (017)** — `run-reference.ts`에 accumulator를 붙였다.
> 1블록 · 253행. 이제 네 파일이 전부 표를 갖는다. 남은 것은 **열 문**(18번)이다.

> ★ **15번 — 화면이 섰다. 그런데 목업에 «파일 이름» 칸이 없다** ★
> 브라우저 실측(2026-08-21 · 데모 DB): 원가 파일이 장부 첫 줄에 뜬다 —
> 「채널 없음 · 기준 데이터 · 원가 대기 171 · 읽지 못한 행 47 · 원가 35 · SKU 신규 35」,
> 되돌리기 버튼 없음(§21-1 · U-3). Fact 3줄은 그대로 되돌리기가 있다.
>
> **다만 표의 머리글이 「시각·연결·유형·대상·조회·신규·갱신·실패·결과·소요」뿐이고
> `Template.tsx`에 하드코딩돼 있다** — 파일 이름을 넣을 칸이 없다. 배선으로 우겨넣으면
> 머리글과 내용이 어긋나므로 **§21 항목으로 올렸다**(남은일 3.11). 대신 정보량이 0이던
> 「유형」 칸(전 행 상수 「파일」)을 진짜 유형으로 바꿨다 — 파일 / 기준 데이터 / 훑기만.
>
> ★ **의존 방향 게이트를 신설했다 (외부 리뷰 지적)** ★ LOCK 4의 *이름* 차원은
> `core-env-neutral`이 127개 시험으로 이미 물고 있었는데, ***import 방향*은 아무도
> 안 물고 있었다.** 실측 0건이라 「지금 0이라 못박는다」로 걸었고, `gates:selftest`에
> `layer-direction`으로 등재해 **부러뜨려 확인했다**(12/12). 새 의존성 0개 · 시험 파일
> 한 곳에 열 줄이다.

> ★ **14번(통)에서 또 「없다」가 틀렸다** ★ ADR-023과 대기목록 8이 「장부는 파일이어야
> 한다」를 **아직 없는 것**처럼 적었는데, **009의 `file_sighting`이 이미 그 척추였다** —
> 앵커의 뜻까지 같은 말로 적혀 있었다(「봤다 → 기록이 남는다」). 새 표를 파지 않고
> 승격시켰다. 진짜 빠진 것은 **`run-reference.ts`가 `recordFileSighting`을 안 부른 것**
> 하나였고, 그래서 원가 파일만 장부에서 빠졌다.
>
> 데모 DB 실측(2026-08-21 · 015 적용 후):
> `2026-01 통합 매출 대시보드.xlsx → pending_cost 171 · bad_rows 47 · cost 35 · sku_created 35`
> 원가 35행 전부 `source_hash`를 갖는다.

> ★ **13번(2.13)에서 처방이 뒤집혔다** ★ 「도서산간배송비를 매핑한다」로 적혀 있었는데,
> 실측이 **배송비가 판매금액합계·공제금액합계 어디에도 없다**는 것을 보였다
> (판매금액합계 = 판매가 + 옵션가 · 공제금액합계 = 서비스이용료(상품) + 할인쿠폰이용료
> + 복수구매할인비용 + 후불광고비 — 셋 다 정확히 일치). **전부 0인 파일로는 부호도
> 귀속도 정할 수 없다.** 그래서 매핑 대신 **「안 받은 컬럼이 돈을 실으면 말한다」**로
> 갔다(`unmapped_money`). 그날 실측이 생기면 그때 부호를 잰다.
>
> 진짜 구멍은 「51컬럼이 수 하나로 뭉개져 0원인지 100만 원인지 구분 못 한다」였다.

> ★ **12번이 닫혔다 (2026-08-21) — 그리고 문서가 또 틀려 있었다** ★
> 결함 51의 「당장의 대처: `contested`로 뜨므로 미리 골라지지 않는다」가 **거짓**이었다.
> 재보니 셋 다 `clear`(미리 선택)였고, **이 결함을 막는 것은 아무것도 없었다.**
> 「동점이면 margin 0」으로 **추론**했는데 실제 비교 대상이 서로가 아니라 상대 채널
> 군집이라 각자 독립으로 `clear`를 받는다. → [ADR-026](ADR-026-구성-표기는-이름-유사도를-이긴다.md)
>
> 처방도 첫 생각이 틀렸다: 「`1+1`을 토큰으로 살린다」는 **부족**했다(0.889 → 0.800,
> 여전히 `clear`). 유사도와 **다른 축**의 관문이 필요했고, 그건 이 파일에 이미 있던
> 「모델 정확 일치가 이름 유사도를 이긴다」의 뒤집은 짝이었다.

<details><summary>왜 이것을 ADR-023보다 앞에 뒀었나 (판단 기록)</summary>

> ADR-023은 **사용자 답 대기**라 내가 못 넘어간다 —
> 앞에 두면 그 자리가 그냥 비어 있게 된다. [2.14](코드-남은일-2026-08-20.md)는 그동안
> 할 수 있고, **열려 있는 것 중 유일하게 사용자를 지금 다치게 한다**: `"1+1"`이 토큰
> 필터에서 통째로 버려져(`listing-match.ts:60-65`) 단품과 **유사도 100% 동점**으로 뜨고,
> 그걸 믿고 합치면 **원가가 다른 두 상품이 한 SKU**가 된다. 되돌릴 수 없다.
> 2026-08-14부터 「판정 대기」였다. 눈에도 보인다 — 연결 화면.

</details>

> ★ **9번의 미루기 사유가 거짓이었다** ★ 「`fact_ad_spend`가 실 DB·데모 둘 다 0행이라
> 만들어도 빈 화면」이라 적혀 있었는데, 실측은 **80,137행 · 15,700,534원**이었다
> (2026-08-21 · 이 기기 · `npm run state`). 커밋된 픽스처만으로 `pnl.ts --clean`이
> 채운다 — 「사용자가 광고 파일을 넣어야 한다」도 참이 아니었다.
> 데모 DB만 0행이 맞다(`demo-db.ts:57`이 8만 행을 일부러 뺐다).
>
> **문서를 믿고 순서를 정하면 이렇게 된다.** 그래서 이 세션이 그 자리들을 기계에 넘겼다.

#### ★ 이 세션이 새로 세운 것 — 드리프트를 사람에게서 기계로 ★

```
finish-gate       Stop 훅이 매 응답마다 dev 게이트를 돌린다 (typecheck·test)   6d40d11
컷 선언 만료       배선된 것을 「안 만든다」고 적으면 붉어진다 · 켜자마자 6건    28f16aa
gates:selftest    등재된 흠집을 넣고 **가드가 물 줄 아는지** 본다 (11건)        711a976
facts             문서의 «저장소 사실»을 기계가 적고 시험이 검사한다            02ed050
expires 태그      한시 문서에 **나가는 문**을 만든다                          40289a1
CLAUDE.md         167 → 111줄. should/is를 파일로 갈랐다 + 자기 규칙 5         f53803d
```

> **왜 이것들이 「눈에 보이는 것 우선」(6-④)을 어기지 않나** — 사용자가 직접 지목한
> 병목이 「이전 세션 문서와 실측의 괴리를 바로잡느라 시간·토큰이 샌다」였다.
> 화면에는 안 보이지만 **다음 세션이 쓰는 시간**에 보인다.

#### 설계 쪽 — ADR-023의 확인 질문 4개

> ★ **2026-08-20 밤에 앱의 정체가 다시 정의됐다** ★
>
> *"나는 지금 정산 대시보드를 위한 **에어테이블+메타베이스**를 만들고 싶은 거 같아."*
> *"**데이터 묶음을 만들기 전까지는 시각화가 되어선 안 됨.**"*
> *"**입구부터 설계가 잘못된 건물 같음.**"*
>
> 그 대화를 [ADR-023](ADR-023-통-묶음-자격-입구순서.md)에 결정 6개로 박았다.
> ✅ **2026-08-21에 넷 다 답을 받았다. 상태는 「결정 · 미구현」이다.**
>
> | | 답 | 그래서 |
> |---|---|---|
> | ① 판정 단위 | **묶음** | §22 판정이 연결이 아니라 묶음 위에서 돈다 |
> | ② 묶음 자격 | **열되 부족분을 말한다** | **비율 절단점을 안 만든다.** `if (ratio < 0.9) return locked` 같은 코드가 나오면 확인을 어긴 것이다. 잠기는 것은 «서류가 아예 없을 때»뿐 |
> | ③ 보관 깊이 | **파싱된 표 전 행** | 원본 바이트는 **안 둔다** — 구매자 실명을 앱이 계속 들고 있게 된다 |
> | ④ 목업 정합 | **기존 화면에 얹는다** | 새 화면을 안 판다. ADR-021이 3커밋 만에 선 그 방식 |
>
> ①(장부)과 ②(보관층)는 **따로 할 일이 아니라 같은 척추의 두 단계**다. 이제 만든다.

이전 판(참고 — ADR-023이 이것을 흡수했다):

> *"파일을 넣으면 그 파일을 우리 앱에서 써먹을 수 있는 테이블로 변환하고,
> 그 테이블을 자유자재로 다룰 수 있게 하는 거"*

> ★ **고르는 기준이 바뀌었다 (2026-08-20 · 사용자 본인)** ★
> *"작업의 우선순위는 일단 앱을 눈에 보이게 고치는 것들부터. 내가 눈으로 변화를
> 확인하지 못하면 흥미도 의욕도 안 생긴다."*
>
> 그래서 아래 목록에 **「눈에 보이나」**를 박았다. 안 보이는 일을 앞에 두려면
> **왜 먼저인지를 그 자리에 적는다** (`/after-work` 6-④).

| | 무엇 | ADR | 눈에 보이나 | 크기 |
|---|---|---|---|---|
| ~~③~~ | ~~**숫자 → 행 드릴다운**~~ ✅ **L2는 섰다** (`src/app/detail.ts`). **L1·L3는 남았다** — 마크업 자체가 없다 ([남은일 3.3](코드-남은일-2026-08-20.md)) | [§21](§21-표면-근거-재배치.md) | ✅ 표 다섯의 행을 누르면 근거 패널이 연다 | ~~M~~ |
| ① | **파일 접수 장부** — 넣은 파일은 전부 한 줄 (통의 얕은 층) | [ADR-023](ADR-023-통-묶음-자격-입구순서.md) 결정 1 · [대기목록 8](다음-마이그레이션-대기목록.md) | ✅ 원가 파일이 기록에 나타난다 | L |
| ② | **원본 보관층** — 파싱된 표 보관 · 아무 파일이나 표로 다시 본다 (통의 깊은 층) | [ADR-023](ADR-023-통-묶음-자격-입구순서.md) 결정 1 | ✅ 넣은 파일을 표로 다시 연다 | L |

~~**③은 ADR-023의 확인을 안 기다려도 된다**~~ — 2026-08-21에 닫혔다. 그 판단은 맞았다:
§21이 이미 결정이었고 배선만 0이라 **ADR을 새로 쓰지 않고** 세 커밋 안에 화면에 섰다.
①②는 여전히 ADR-023의 확인 질문 4개가 먼저다(스키마가 늘기 때문).

### 조사가 찾아 둔 것 — 47건 (2026-08-20 밤 · 6개 렌즈 전수)

아직 문서로 안 옮겼다. **다음 세션이 이 목록부터 옮긴다.** 1순위로 올라온 것 중
오늘 닫은 것(팝오버 16곳·문구 3건)을 뺀 나머지에서 큰 것들:

```
정산 「일치」가 금액을 안 본다   `settlement.ts:152` — 판정이 「주문에 이어졌나」뿐인데
                              화면 부제는 「마켓 정산서 대사」다. recon.ts는 src/에서 0곳 사용
「지금」이 전부 UTC            `data.ts:183·263·486` · `App.tsx:199` — ADR-009는 KST 자정이라 적었다.
                              매월 1일 아침 00~09시에 열면 지난달이 뜬다
드롭존이 가짜               「여기에 끌어다 놓으세요」인데 onDrop 0건. Tauri dragDrop이 꺼져 있어
                              웹뷰가 그 파일로 내비게이션할 수 있다 — 첫 사용자의 첫 동작
백지 화면 셋               설정·데이터 구조·내 필드가 「준비 중」조차 없이 빈다.
                              UNBUILT에 diag만 있어서 ⌘K가 이 셋으로 안내한다
표 5곳의 행이 눌리는 척       ✅ 닫혔다 (2026-08-21) — 다섯이 전부 L2 근거 패널로 내려간다
주문 화면 품목이 «—»         주석은 「fact_order_item이 비었다」인데 거짓 — 적재된다.
                              조회 SQL(`order/rows.ts:113`)에 조인이 없을 뿐
```

~~**③이 제일 싸다**~~ — 그리고 실제로 그랬다. 마크업·타입·CSS가 전부 있었고 배선만 0이라
**새 마크업 없이** 이었다. U-6 래칫(`DEAD_ROW_CLICK_MAX`)이 5에서 **0**으로 내려갔고,
§21-5(「derived 값은 근거 경로 없이 렌더되지 않는다」)와 코드의 어긋남이 그만큼 줄었다 —
**다 없어진 것은 아니다.** L1·L3는 마크업이 없어 아직 남아 있다 (남은일 3.3).

> ⚠ **①②에는 ADR이 없다. 손대기 전에 ADR부터 쓴다** (작업 리듬 4).
> 같은 목록에 나란히 있던 「묶음」은 [ADR-021](ADR-021-묶음-무엇을-합쳐-볼-것인가.md)을
> 받아 3커밋 만에 화면에 섰고, 「원본 보관층」은 두 줄로 남아 열흘을 떠 있었다.
> **차이는 그것 하나였다.**
>
> 그리고 ①을 「원가도 batch를 쓰게 한다」로 풀지 않는다 — 이유는 대기목록 8에.

**전면 재설계는 하지 않기로 했다** — 엔진(파서·판정·적재·손익)은 실파일에서 일한다.
문제는 표면이고, 근거 층은 그 위에 얹는 증축이다.

### 그다음 — 작지만 열려 있는 것

```
죽은 버튼 둘           「내 필드」 primary(`addCf`) · 채널 「연결 관리」(`manage`)
                      둘 다 §20 게이트 안이라 **「안 그리기」가 답일 수 있다** —
                      U-3은 「못 누르는 것은 아예 안 그린다」이므로 지우는 것도 이행이다
B-3 메모리 판정        절대 상한 256MB가 코드가 아니라 **환경**을 재고 있다
                      처방: 행 수·시간은 CI로 분리 · 메모리는 환경별 기준선 대비 상대 판정
                      감사 항목 중 **통째로 남은 유일한 것**이다
A-4-3 개인 프로파일    `data.ts`가 세어 놓고 `console.warn`으로 끝낸다
                      `user-invariants`의 `WARN_MAX = 3` 래칫이 세고 있다
A-4-5 §18-A 경고 문장  수식 비율 %와 색만 있고 「원본이 따로 있다면 그쪽이 안전합니다」가 없다
```

### 사용자만 할 수 있는 것

> ⚠ **여기 숫자를 인용하기 전에 `npm run state`를 먼저 돌린다.**
> 이 절의 값 둘이 실제로 틀려 있었다 — 「실기기 DB는 13」(실측 **11**) ·
> 「리스팅 190건이 전부 미연결」(실측 **linked 236 · 미연결 0**). 둘 다 다른 기기에서
> 잰 값이 이 기기의 사실인 척 적혀 있었다. **기기가 다르면 숫자도 다르다.**

| | 무엇 | 명령 · 왜 |
|---|---|---|
| ① | **시험용 원가 2건 삭제** — 8월 손익이 틀린다 | `npx tsx tools/harness/cost-remove.ts --sku SKU-0001,SKU-0002 --on 2026-08-16 --amount 1234`<br>`--yes` 없이 먼저 보여준다. 되돌릴 수 없다(`row_shadow`가 없다) |
| ② | ~~실기기 `npm run e2e` 3회~~ **끝났다** | 2026-08-20 21:40 실기기: e2e 5.8·5.7·5.6초 / +237.2·+235.7·+215.4MB · perf 2.7·2.7·2.7초 / +205.9·+220.9·+211.8MB. **6회 전부 통과.** 행 수 3중 일치 80,137 (3회 동일) |
| ③ | **리스팅을 SKU에 연결** — ⚠ **이 줄이 두 번 낡았다** | 「끝났다(linked 236 · 미연결 0)」로 적혀 있었는데 그건 **2026-08-20 다른 기기** 값이다. 이 기기는 **2026-08-21 15:28 · `npm run state`** 기준 `리스팅 unlinked 233 · SKU 0 · 원가 이력 0`이다. 그리고 같은 줄의 「`fact_ad_spend` 0행」도 **거짓이다** — 이 기기 실측 **80,137행**. **숫자를 인용하지 말고 `npm run state`를 돌린다** |
| ④ | **내보내기 파일 이름 확인** | 데스크톱에서 CSV/XLSX를 한 번 눌러 본다. 앵커에 `"주문 2026-07.csv"`가 붙는 것은 확인했는데, **헤드리스 브라우저가 `download` 속성을 안 읽어** 저장된 이름을 끝까지 못 봤다 |
| ⑤ | **앱을 한 번 열어 스키마를 올린다** (2026-08-21 신설) | 이 기기 실 DB가 **스키마 14**인데 정본이 **16**이다 (`npm run state`가 `⚠ 정본 16`으로 표시). 앱을 열면 `catchUp`이 015·016을 적용한다. 내가 못 하는 이유는 Tauri 창을 띄우는 일이라서다.<br>⚠ **올린다고 예전 파일의 표가 생기지는 않는다** — `sheet_block`은 **새 가져오기에만** 쌓인다. 광고 파일을 표로 보려면 다시 넣어야 한다 |
| ⑥ | **원가 파일을 실 DB에 넣어 본다** | 이 기기 실 DB는 `SKU 0 · 원가 0`이라 상품별 손익이 「SKU에 연결되지 않은 판매」 한 줄뿐이다. 원가를 넣어야 **L2 근거 패널·통 장부**가 제 값을 보인다. 지난 세션들이 결함을 캔 방식이 그것이었다 (앱을 켜고 파일을 넣다가) |

## ★★ 이 세션 (2026-08-21 · 세션 4부) — 커밋 6개 ★★

```
6116dd0  표 다섯이 갈 곳을 얻는다 — §21-2 L2 근거 패널 (조사 1.7)
ec6f4ce  「이것을 막고 있는 것이 있다」가 거짓이었다 — 결함 51 재실측
eea164b  구성 표기가 이름 유사도를 이긴다 — 「1+1」 동점 (ADR-026)
981c606  안 받은 컬럼에 돈이 실리면 말한다 (조사 2.13)
968e707  ADR-023이 «결정»이 됐다 — 통·묶음·입구 순서에 답이 왔다
c028770  통을 세운다 — 그런데 장부는 이미 있었다 (015)
b353e6c  장부가 파일 단위가 된다 + 의존 «방향»을 처음으로 못박는다
4b10973  원본 표를 담는다 — 재보니 gzip 블록이 42배 작았다 (016 · ADR-027)
```

### ★ 이 세션이 배운 것 — **네 번 「없다/안전하다」가 틀렸다** ★

이 세션의 진짜 소득은 기능이 아니라 **같은 실패 모양을 네 번 잡은 것**이다.
전부 «재보지 않고 쓴 is-문장»이었다:

| 문서가 말한 것 | 실측 |
|---|---|
| 「1+1은 `contested`라 미리 안 골라진다 — 피해를 막고 있다」 | **셋 다 `clear`**였다. 막는 것이 **아무것도 없었다** |
| 「도서산간배송비를 매핑하면 된다」 | 배송비가 두 집계 **어디에도 없다.** 전부 0인 파일로는 부호를 못 정한다 |
| 「파일 장부를 새로 만들어야 한다」 | **009가 이미 그 척추였다.** 빠진 건 호출 한 줄 |
| 「drift 게이트가 off · 규범은 습관」 (외부 리뷰) | LOCK 4 *이름* 차원은 **127개 시험**이 이미 물고 있었다. *방향*만 무방비였다 |

> **공통점: 넷 다 «추론»이었다.** 「동점이면 margin 0이니까」 · 「컬럼이 있으니까」 ·
> 「없다고 적혀 있으니까」 · 「gate가 안 보이니까」. **재는 데 든 시간은 매번 5분 미만**이었다.

### ★ 그리고 세 번 「첫 처방」이 부족했다 ★

- 「`1+1`을 토큰으로 살린다」 → 0.889 → **0.800, 여전히 `clear`.** 다른 축(관문)이 필요했다
- 「배송비를 매핑한다」 → 추정이 된다. **「말한다」가 답이었다**
- 「표를 평문으로 담는다」 → 43MB. **gzip 블록이 42배 작았다**

세 번 다 **계산하거나 재서** 버렸다. 그중 둘은 나중 실측이 계산을 확인해 줬다
(0.80 · 1.02MB).

<details><summary>이 세션 앞부분 기록 (L2 근거 패널)</summary>

## ★★ 이 세션 (2026-08-21) — §21-2 L2 근거 패널 ★★

**표 다섯의 행이 갈 곳을 얻었다.** 대시보드 상품별 · 채널별 · 주문 · 정산 · 상품 —
전부 `click: () => {}`이었고 `cursor:pointer`만 그려 놓고 있었다.

| 무엇 | 한 줄 |
|---|---|
| **`src/app/detail.ts` 신설** | 다섯 화면의 항 분해 · 데이터 출처를 한 곳에서 만든다. **새 마크업 0** — `<aside className="detail-side">`도 CSS도 반응형 분기도 처음부터 있었고 배선만 없었다 |
| **`DEAD_ROW_CLICK_MAX` 5 → 0** | U-6이 세던 「갚을 빚」이 사라졌다. 이제 이 상한은 **되돌아가지 못하게 하는 못**이다 |
| **컷 오분류 정정** | `detail`·`hasDetail`·`closeDetail`이 「미구현 화면 — 설정·라이선스 · 배포 단계 일」로 선언돼 있었다 (남은일 4.1-①). 배선 255→258 · 컷 214→211 |
| **`tests/detail-panel.test.ts` 28건** | 패널이 **실제로 렌더되는지**부터 본다 — 이 마크업은 한 번도 서 본 적이 없었다. `screen-safety`의 `VIEWS`에도 한 줄 더했다 |

### ★ 이 세션이 내린 판단 셋 ★

1. **패널은 값이 아니라 «주소»를 든다.** 열어 둔 채 가져오기·되돌리기가 일어나면
   복사해 둔 숫자는 즉시 거짓이 된다 — **표와 패널이 다른 말을 하는 것**이 이 저장소가
   이미 두 번 겪은 실패다(주문 품목 «—» · 할인 3단). 그래서 매 렌더에 표와 같은
   배열에서 다시 찾고, 대상이 기간 밖으로 나가면 패널이 안 뜬다.
2. **§21-2가 적은 것을 다 만들지 않았다.** 「이번 달 달라진 것 4종」과 「다음/이전
   연속 이동」은 **동결 마크업에 슬롯이 없다.** §21이 목업을 이기지만 그 예외는 §21이
   **명시한 목록**에만 열린다 — 없는 요소를 지어내면 E-1 위반이다. 안 만든 것과
   그 이유를 `detail.ts` 머리에 적었다.
3. **부재는 패널에서도 0이 아니다.** 상품에 배분 안 한 수수료·배송비·광고비는 «—»이고,
   정산이 안 이어진 채널의 `fee: 0`은 「0이 아니라 «모름»입니다」라고 말한다 (§22 · LOCK 6).
   **데모 DB에서 실제로 그 문장이 떴다** — ESM 채널이 정산 파일 없이 서 있다
   (2026-08-21 · 브라우저 · localhost:5173). 실 DB는 이 기기에서 원가·SKU가 0이라
   같은 화면이 다르게 보인다 — 숫자를 인용하기 전에 `npm run state`.

> **왜 이것이 「눈에 보이는 것 우선」(6-④)에 맞나** — 사용자가 대시보드 숫자를 눌러
> 그 숫자가 어디서 왔는지 본다. 지금까지는 눌려도 아무 일이 없었다.

---

</details>

## 직전 세션 (2026-08-20 밤 · 실기기) — 화면 6건 · 명령어 · ADR-023

사용자가 실기기에서 앱을 켜고 원가 파일을 넣다가 시작됐다.

| 무엇 | 커밋 | 한 줄 |
|---|---|---|
| **앱의 거짓말 셋** | `dfbe88b` | 「팔리면 그때 붙습니다」는 **거짓**이었다 — 매출을 넣어도 `run.ts`는 `pending_cost`를 다시 안 훑는다. 없는 열 이름(「상품번호」)·기록 경고가 결과 뜨면 사라지던 것도 |
| **팝오버 16곳** | `8558cd0` | `stop`·`stopEvt`·`openCmd`가 전부 `() => {}`였다. **팝오버가 자기가 연 것을 자기가 닫고 있었다** — 마우스만 쓰면 ⌘K에 한 글자도 못 쳤다. 배선 246→249 · 컷 223→220 |
| **명령어 보강** | `37bc49a` | `npm run state` 신설 + `/before-work` 3·4 + `/after-work` 6-①②③④ + 대기목록 8 |
| **ADR-023** | (이 커밋) | 통 · 묶음의 자격 · 입구 순서 · API 자리. **제안 상태** |

### ★ 이 세션이 배운 것 — 다음 세션이 되풀이하지 말 것 ★

**① 문서의 숫자는 기기를 건너면 거짓이 된다.**
「실기기 DB 13」(실측 11) · 「리스팅 190건 미연결」(실측 미연결 0). 둘 다 클라우드
컨테이너에서 잰 값이었다. 규칙은 있었다 — **깨진 것만 재고 안 깨진 건 믿었다.**
그래서 `npm run state`를 만들었다. 규율은 「재라」로는 안 지켜지고 한 줄로만 지켜진다.

**② 미룬 것은 미루는 자리에 적어야 한다.**
ADR-019가 「대기 목록으로 남긴다」고 약속했는데 대기목록에 그 항목이 **없었다.**
그래서 열흘 떠 있었다. 이력 발굴 결과 **되돌려진 게 아니라 한 번도 착수된 적이 없었다**
(`git log --all -S "openBatch" -- run-reference.ts` → 빈 출력).

**③ 요약이 설계를 대체하면 다음 세션이 어긋난 것을 만든다.**
같은 「다음 손」 목록의 「묶음」은 ADR-021을 받아 3커밋 만에 화면에 섰고, 「원본 보관층」은
두 줄로 남아 못 알아봤다. **차이는 그것 하나였다.** ADR-023이 같은 실수를 막으려 존재한다.

**④ 게이트가 녹색이어도 마우스는 못 쓴다.**
`stopEvt` 14곳이 죽어 있는 동안 typecheck·1152 시험·wiring 100%가 전부 녹색이었다.
U-3 게이트는 `onClick={vals.…}` **패턴만** 보고 핸들러가 무엇을 하는지는 안 본다.
브라우저에서 실제로 눌러야 나왔다 (작업 리듬 7의 새 사례).

**⑤ 「제자리」가 아니었다.** 10일 연속 192커밋, 빈 날 0일. 안 닫힌 건 이 한 자리뿐이다.

---

## 이전 세션 (2026-08-20 낮·저녁) — 감사를 코드로 소비했다

| 무엇 | 커밋 | 한 줄 |
|---|---|---|
| 결함 65 · 원가 삭제 · 묶음 v1 | `26c431b`·`ec38bcd`·`d933bb9`·`0658a69` | (세션 앞부분) |
| **문서 감사 + 정리** | `e8ea6ec`·`097ecdd` | 12,294줄 대 코드 전수 대조. 읽는 길 5,344 → 649줄 |
| **F절 — 산문을 게이트로** | `a2b3791`·`8e9f239` | 사용자 불변식 U-1~U-8 + 시험 · 잠금 침묵 11자리 · 매니페스트 골든 대조 |
| **CI 스텝 둘** | `da5b1fb`·`6362b3d` | 화면 단언 33건이 깨어났고 **깨우자마자 낡은 단언 하나가 붉어졌다** |
| **할인 3단** | `1b14e78` | 상품별은 빼는데 전사는 안 뺐다 — 값이 오면 두 화면이 갈린다 |
| **768px 「더보기」** | `af65fbc` | 배선하고도 안 됐다. **탈출구가 자기를 지우는 규칙에 같이 지워지고 있었다** |
| **광고비 상품 배분** | `77d9b94` | 014 · ADR-022. `adDirect: 0` 리터럴을 없앴다 |
| **내보내기 5종** | `a497f0f` | LOCK 8이 「전면 개방」인데 전면 부재였다 |
| **⌘K** | `62b7286` | 마크업은 통째로 있고 리스너만 0이었다 |
| **낡은 컷 선언 둘** | `389e11b` | 내 작업이 만든 거짓말을 같은 날 닫았다 |

### ★ 이 세션이 배운 것 — 다음 세션이 되풀이하지 말 것 ★

1. **소스 검사는 「그렸나」를 보지 「보이나」를 못 본다.** 768px 「더보기」는 배선을
   붙이고 시험까지 통과시킨 **뒤에도** 화면이 그대로였다. 잡은 것은 브라우저다.
   화면을 만졌으면 **브라우저로 눌러 본다.**
2. **게이트가 못 보는 배선은 게이트를 거짓말하게 만든다.** `vals[FIELD[key]] = …`로
   다섯 자리를 돌렸더니 배선 게이트가 5건을 미배선으로 신고했다. 계산된 키를 쓰지 않는다.
3. **컷 선언은 낡는다.** 이번 세션에만 넷을 고쳤다(`exp[A-Z]`·`toggleHeadMore`·
   광고비 사유·커맨드팔레트). 무언가를 만들면 **그것을 덮던 컷을 반드시 다시 읽는다.**
4. **자해하지 않은 게이트는 게이트가 아니다.** 통과를 보는 것으로는 아무것도
   증명되지 않는다. 이번에 부러뜨린 것 열넷이 전부 붉어지는 것을 봤다.
5. **`ALTER TABLE`은 그림자 트리거를 갱신하지 않는다.** SQLite가 트리거 본문을
   텍스트로 보관하기 때문이다. 컬럼을 늘리면 `shadow_*`를 **다시 만든다**
   (`tests/shadow-columns.test.ts`가 이제 전수로 지킨다).

### ★ CI가 실제로 판정하는 것 (2026-08-20 저녁) ★

```
npm ci · lock diff · typecheck   종전
pnl.ts --clean            9초   ← 새로 추가. 화면 단언 33건을 깨운다 (B-2)
npm test                2분37초   34 skipped → 1
npm run build             25초   ← 새로 추가. 빌드 실패의 첫 발견자가
                                   베셀 배포였다 (B-7)
```

**러너에서 실측했다** (run #79 · `da5b1fb` · 3분 40초):

```
Test Files  84 passed (84)
     Tests  1118 passed | 1 skipped (1119)      ← CI 자신의 출력
```

씨딩 스텝이 초록인 것만으로는 부족하다 — 파일을 만들고도 시험이 계속 skip이면
초록은 거짓이다. **그래서 로그의 숫자를 봤다.** 남은 1건은 `#8 원본`(raw 픽스처)이고
`fixtures/raw/`가 커밋되지 않으므로 CI에서는 영원히 skip이다.

배선은 CI에 안 건다 — 판정자는 `wiring-coverage.test.ts`의 `TODO_MAX = 0`이고
그건 `npm test`가 이미 돌린다. 대신 CLI에 `--check`를 붙였다(B-5). 그전까지 그
명령은 **남은 일이 100건이어도 exit 0**이었다.

### ★ F절 — 무엇이 게이트가 됐나 ★

```
사용자 불변식 U-1~U-8        docs/사용자-불변식.md + tests/user-invariants.test.ts (15건)
잠금 침묵 11자리             전부 sayBusy로 말한다. 침묵하려면 「침묵 OK」로 선언
가져오기 입구 수             둘뿐(runImport·runReferenceImport). 셋째가 생기면 멈춘다
매니페스트 골든 대조         tests/manifest-golden.test.ts — 세션 1 합격 기준 ①이 깨어났다
ADR 템플릿 ★파급면★          결정이 닿는 표면을 세는 단계. LOCK 열 줄을 한 번씩 묻는다
```

> ★ **자해 시험을 통과한 것만 위에 적었다** ★ 게이트는 통과하는 걸 봐서는 아무것도
> 증명되지 않는다 — **일부러 부러뜨려 붉어지는 걸 본 것만** 게이트다.
> 이번에 부러뜨린 것: 말하던 가드를 침묵으로 되돌리기 · **주석에만 `sayBusy`를 적기** ·
> 「침묵 OK」 남발 · 화면의 «장부에 안 나온다» 문장 지우기 · 셋째 가져오기 입구
> 추가하기 · 헤더 확신도를 바꿔 파서 회귀시키기. **여섯 전부 붉어졌다.**
>
> 그중 하나는 **골든 대조 시험이 자기가 대조할 정답지를 덮어쓰고 있었다** —
> `build-manifest.ts`를 import 하는 것만으로 CLI가 돌았다. `isMain` 가드로 막았다.

### ★ 묶음 — 브라우저 실측 ★

```
전체            매출 8,285,200원 (2026년 7월)
「정산만」       매출 0원          ← 주문이 없는 파일만 담았을 때
전체 복귀       매출 8,285,200원
콘솔 오류 0 · 「계산 밖 매출 39,506,300원 (전 기간)」이 막대에 뜬다
```

### ★ `main` 병합 완료 — 프로덕션이 오늘 작업을 그린다 ★

```
main            4aa7945 → 0658a69   fast-forward (충돌 0 · 이력 안 잃음)
베셀 프로덕션    같은 커밋으로 자동 배포
```

부채 목록의 「1. `main` 병합」이 이걸로 닫혔다. **웹판은 SSO가 걸려 있어 소유자만 본다**
(`ssoProtection: all_except_custom_domains` · 커스텀 도메인 없음).

> ★ 병합 전에 확인한 것 — **마이그레이션은 전부 되거나 전부 안 된다** ★
> `applyMigrations`가 마이그레이션마다 `db.transaction`으로 감싼다. 12까지 올린 DB에
> 013을 **일부러 터뜨려** 실측했다: 버전 12 · 뷰 5개 · `collection` 표 0개 ·
> `active_order` 조회 정상 — **흔적이 0이다.** 뷰가 사라진 반쪽 DB는 안 생긴다.
> 그 뒤 정상 013을 올리면 아무 일 없이 버전 13이 된다.
>
> ⚠ 그래도 **실기기 데이터로는 013·014가 아직 안 돌았다.** 앱을 처음 켜기 전에
> `cp .tmp/pnl.sqlite .tmp/pnl.sqlite.bak-0820` 한 줄은 뜨고 가는 게 낫다 —
> 잃을 것이 31MB 실데이터다.
>
> **014는 `ALTER TABLE` + 트리거 재생성이다.** 되돌리기 그림자가 `listing_key`를
> 담도록 `shadow_fact_ad_spend`를 지우고 다시 만든다 — 그 순서도 한 트랜잭션 안이다.

### 남은 것 (묶음)

- **v2 스마트 묶음**(규칙형) · **v3 혼합** — ADR-021 그대로
- **연결 정체성** — `conn-${marketplaceKey}`라 11번가 계정 둘이 한 연결이다. **별건**
- 「묶음 이력」은 쌓이지만 **보는 화면이 없다** (`collection_event` · 읽는 메서드는 있다)

---

---

### ★ 남은 대열 ★

| | 무엇 | 왜 여기 |
|---|---|---|
| ~~3~~ | ~~비치명 오류의 목적지~~ | **완료 (2026-08-15)** — 위 «비치명 오류의 목적지» 절 |
| **4** | ~~③-a undo 비대칭~~ · ~~③-b abort·적재 중~~ **완료** · 남은 것: **`signPolicy` 집행 + 음수 수량** | 「중간 실패 상태 3건」의 나머지는 **문서에 없다**(위 절) — 확인 필요 |
| ~~등재~~ | ~~재시도가 미완료 배치의 정리를 막는다~~ | **완료 (2026-08-16)** — 위 「재시도가 곧 정리다」 절 |
| ~~4-②~~ | ~~Tauri 연결이 전역 하나~~ | **완료 (2026-08-16, MVP 3)** — 임차 번호 계약. 이미 임차 중이면 열기를 거절하고, 명령은 자기 번호가 아니면 듣지 않는다. 화면 잠금도 `acquire`/`release` 하나로 합쳤다 |
| 등재 | **DB 경로가 빌드에 박혀 있다** (`__PROJECT_ROOT__`) | 폴더를 옮기면 앱이 DB를 못 찾고, exe만 복사한 기기는 빈 화면이다. 고치려면 OS 앱데이터로 **31MB 실데이터 이사**가 함께 간다 — 실제 배포 때 (2026-08-16) |
| ~~등재~~ | ~~**원가 이력을 지울 경로가 없다**~~ → ✅ **닫혔다** (2026-08-20) — `Repository.removeCost` + `tools/harness/cost-remove.ts`. **앱 화면에는 아직 없다** (도구뿐) | 잘못된 **날짜**로 넣으면 되돌릴 방법이 없었다. 2026-08-16에 실제로 일어났다(시험용 1,234원 2건) |
| 등재 | `openBatch`~`commitBatch`에 트랜잭션이 없다 | 데이터는 안전하다(`active_*`가 거른다). 이제 잔존물은 재시도가 치운다 — 남은 것은 「애초에 안 남기기」 |
| ~~등재~~ | ~~**대시보드 「상품별 손익」 미배선**~~ → ✅ **닫혔다** — `dashboard.ts`가 `vals.pnlRows`를 채운다. 이 줄은 2026-08-20 마감 점검에서 **낡은 것으로 확인**됐다 | 낡은 부채 줄이 남으면 다음 세션이 이미 된 일을 다시 판다 |
| 등재 | **앱에 원가 일괄 입력이 없다** — 오늘은 `tools/harness/cost-import.ts`로 넣었다 | 사용자가 46행을 손으로 치다 막혔다. 다음 달에 또 반복된다. 위저드의 파일 입력 경로(ADR-013)를 재사용할 수 있다 |
| 등재 | Validation 단계 부재 (위 «인지된 부채») · 표면 4건 | 3번이 다이제스트를 만질 때 표면 둘은 같이 풀릴 수 있다 |
| ~~등재~~ | ~~앱이 PRAGMA를 안 건다 · `busy_timeout = 0`~~ | **완료 (2026-08-16, 4번 선행)** — [ADR-014](ADR-014-열기-설정의-세-갈래.md). 켜기 전 `foreign_key_check` 실측 **0건**, 그 뒤 켰다 |

> **4번의 처방은 «일괄 `abs()`»가 아니다** (사용자 확정, 2026-08-15). ADR-009 ②의
> 원문이 「Normalization이 절대값 + **타입**으로 정규화」이므로, 음수의 의미(공제냐
> 환불이냐)는 **프로파일이 선언하고 집행자는 그것을 읽는다.** 그리고 **선언 없는
> 음수는 비치명 오류로 표면화**한다 — 그래서 3번이 4번의 선행 조건이었고, 이제
> 그 통로는 `ISSUE_KINDS`에 줄을 더하는 일이 됐다.

### `signPolicy` 실측 — 부채가 아니라 잠재 결함이다

```
① 오늘 픽스처 4종 매핑 결과에 음수 금액  0건
② 합성 음수 -12345 → 오류 없이 -12345 그대로 저장   (abs 없음 · Normalization 무개입)
```

ADR-009 ②(「저장은 양수, 의미는 타입이 정한다」)의 **집행자가 없다.** 오늘 안 터지는
이유는 픽스처가 우연히 음수를 안 실어서다 — 11번가 정산의 공제금액이 음수로 오는
양식이 하나 생기면 그날 터진다. D(음수 수량)와 같은 계열이라 **4번 대열**에 넣는다.

**새 세션을 열면 이 문서부터 읽는다.** 어디까지 됐고, 무엇이 막혀 있고, 다음 손이
어디로 가야 하는지가 여기 있다.

> **이 문서의 경로 표기는 `src/` 기준이다.** `core/linking/view.ts`는 실제로
> `src/core/linking/view.ts`이고 `packs/…`는 `src/packs/…`다. 다른 기기에서
> 실측으로 확인하다 어긋나 보일 수 있어 적어 둔다 — `tools/`·`tests/`·`docs/`·
> `mockup/`만 저장소 루트 기준이다.

---

## 지금 어디인가

| 세션 | 상태 |
|---|---|
| **세션 1** 파이프라인 | **합격** (2026-08-11 선언) |
| **세션 2** 저장 골격 | **종결** (2026-08-12 선언, `fb78c2a`) |
| **세션 3** 접합 | 진행 중 — 화면 **6개** 배선 + 가져오기 위저드 · 2d 완주(연결 86/86) · **③④ 원가** |

<details>
<summary>세션 셋이 무엇이었나 — 정의와 합격 기준 (2026-08-21에 <code>CLAUDE.md</code>에서 옮겨 왔다)</summary>

> **왜 옮겼나** — 이건 **규범이 아니라 상태**다. 세션 1·2는 이미 닫혔고 합격 기준의
> 숫자(30초 · 256MB)는 `npm run perf`가 **이미 강제한다**. 상시 로드되는 `CLAUDE.md`에
> 두면 매 세션이 「닫힌 계획」과 「하네스가 이미 재는 숫자」를 먼저 읽는다.
> 남은 규범 한 줄(**세션을 건너뛰지 않는다**)만 그쪽에 남겼다.

- **세션 1** 파이프라인 (UI 없음) — 스니퍼 + 파서 4종 + Extraction + Normalization + 픽스처 하네스
  - 합격: 픽스처 15개 전부 `expectedProfile`/`expectedSheets`/`expectedHeader`/`expectedRowCount` 통과
  - 합격: 쿠팡 광고 픽스처(80,138행) 파싱+적재 **30초 이내** (진행 표시 포함)
  - 합격: 같은 픽스처 처리 중 **Worker 피크 RSS 256MB 이하**. 초과는 실패가 아니라 "SheetJS 전량 적재의 한계" 판정 — Extraction만 Rust 포팅하는 근거가 된다
  - 검증: 뽑은 `expected*` 매니페스트를 [픽스처-대조표](docs/픽스처-대조표.md)와 교차 대조. 불일치 항목만 사람이 판정한다 (행 수 정의 차이 = 물리 행 vs 데이터 행은 불일치가 아니므로 먼저 구분)
  - 선행: 픽스처 비식별화(구매자명·연락처·주소 치환). 원본 커밋 금지
- **세션 2** 저장 골격 — Tauri + SQLite, 스키마 전부, append-only batch, 리포지토리 계층
- **세션 3** 접합 — 목업 몸체 이식, 시드→리포지토리 교체, 실파일 통과, 손계산 정답지 대조

세션을 건너뛰거나 한 세션에 일괄 위임하지 않는다.

</details>

> **★ 새 세션이 제일 먼저 볼 곳 ★**
>
> **7월 리허설은 통과했다** (11번가·ESM 2종 · 아래 «리허설 결과»).
> 남은 두 파일(19시트 · 쿠팡 8만 행)은 **새 화면을 보는 것**이라 정보값이 다르다.
>
> **[§22 커버리지](§22-커버리지-부재의-일급-취급.md)가 구현됐다** — 채널 화면이
> 처음으로 배선됐고, 앱이 *"정산 파일이 없어 수수료가 손익에 잡히지 않습니다"*를
> **먼저 말한다.**
>
> **★ 실기기 한 바퀴가 돌았다 (2026-08-14) ★** 사용자가 쿠팡 매출 2종을 실제로
> 넣었고 광고 배치를 되돌렸다. **73,740,340원이 대시보드에 합류했다.**
>
> ```
> 주문 434행 · 매출 86,909,220원      (ESM 146 + 쿠팡 일반 141 + 쿠팡 제트 147)
> 클레임 9 · 정산 128 · 광고 0        ← 광고 batch 되돌림 (완료 기준 6 실행)
> 리스팅 261 · 연결 79 · SKU 61 · batch 7(undone 1) · 기간집계 147행
> ```
>
> **★ 실기기 3종 통과 (2026-08-14, 사용자 확인) ★**
>
> ```
> ① 쿠팡 매출 2종 → 대시보드 반영. «담지 못한 것» 카드가 가짜 큰 숫자를 9개
>    항목으로 정직하게 설명 — 사용자 채점 (a) «경고가 숫자와 함께 바로 보였다»
> ② 되돌리기 한 바퀴 완주        → 완료 기준 6 닫힘
> ③ 18시트 파일 → 시트 고르기 · 프로파일 부재 정직 거절 · 제외 4건 사유별 표시
>                                → §18 첫 실전 합격
> ```
>
> **★ ③④가 들어왔고, 그다음 품목 적재가 자물쇠를 풀었다 ★**
>
> ```
> ③ 원가 입력 UI (상품 화면)     ✅
> ④ costAt → computePnl          ✅ 배선 · ✅ 값   ← 품목 적재로 닫혔다
> 품목 2단계 적재                 ✅ ESM 146 · 쿠팡 일반 141 · 제트 147
> ```
>
> **원가 1건이 순이익을 정확히 «수량 × 원가»만큼 깎는 것을 실파일로 시험한다**
> (`tests/order-item.test.ts` ④). «적용 불가» 문구는 스스로 사라졌다.
>
> ⚠ **기존 DB에는 품목이 없다.** 품목은 적재 시점에 생기므로 이미 들어온 batch는
> 그대로다 — 실기기에서 보려면 **7월 파일을 다시 넣어야** 한다(UPSERT라 행 수는
> 안 늘고 품목만 새로 생긴다). 그때까지 그 DB는 계속 «적용 불가»라고 말한다.
>
> ### ★ 다음 실기기 세션 — 순서가 중요하다 (사람) ★
>
> **선행: 클레임 이중 기록이 랜딩된 뒤에 시작한다** (커밋 `ba7b9c5` — 완료).
> 그 수정으로 ESM 매출·순이익이 **+388,700원** 움직인다. 원가를 넣으며 보는 숫자가
> 도중에 바뀌면 «내가 뭘 잘못 넣었나»가 되므로, **바닥이 안정된 뒤에** 넣는다.
>
> | | 무엇 | 볼 것 |
> |---|---|---|
> | ~~①~~ | ~~**7월 파일 재가져오기**~~ → **완료 (2026-08-16)** — 4종(11st 포함)을 `raw`로. 품목 434 생성. ⚠ 행 수는 **9행 늘었다**(이중 기록) — 위 「실기기 ① 완료」 절 |
> | ② | **원가 실입력** — 주력 SKU 몇 개부터, 실제 원가표로 | **순이익이 진짜 방향으로 처음 움직인다** |
> | ③ | 상품 화면 | 「판매 수량」이 SKU별로 선다 |
>
> ⚠ **②에서 적용 시작일을 7/1(또는 그 이전)로 내려야 7월 손익이 움직인다.**
> 기본값은 오늘(8월)이고, 그대로 넣으면 7월은 0이다 — **버그가 아니라 «오늘부터 이
> 원가»라는 사용자의 주장을 앱이 존중하는 것**이다. 이제 게이지가 그 상태를 정직하게
> 구분한다(기준일 = 보고 있는 기간).
>
> **①번 숙제(«원가를 넣으면 순이익이 나온다»)의 완제품이 이것이다.** ②가 이
> 프로젝트에서 처음으로 «사람이 넣은 값이 손익을 바꾸는» 순간이다.
>
> **결함 53은 고쳤다** — 확인 화면이 이제 컬럼의 역할을 전부 말한다. 판정은
> `core/import/mapping/index.ts`의 `columnRoles()` 하나로 모였다.
> [§20 보강](§20-보강-확인-질문.md)은 설계만 — 트리거 대기.
>
> `.tmp/pnl.sqlite`가 없으면 화면 테스트가 건너뛰어 `npm test`가 491이 아니라
> 적게 나온다. **주의**: `npx tsx tools/harness/pnl.ts`는 DB를 새로 만들므로
> **2d에서 만든 연결이 사라진다.** 지금 기기에는 이미 있으니 다시 돌리지 않는다.

### 세션 2 합격 기준 현황

| # | 기준 | 상태 |
|---|---|---|
| 1 | 적재 → 되돌리기 → 행수 원복 | **통과** |
| 2 | 종단 #13 30초 / 256MB | **통과** — 12회 전부. 단 여유 6.3MB, 아래 참조 |
| 3 | 되돌리기 후 재가져오기 시 조정 생존 | **통과** |
| 4 | 활성 로드 (전체 테이블 적재 경로 부재) | **통과** — 구조로 증명 |

기준 2는 2026-08-12에 **개정**됐다 (헌장 D부). 판정 = JS 파이프라인 축소 후 재측정.
드라이버 교체는 별건으로 강등 — 세션 3의 Tauri 통합 시점에 확정한다.

---


---

## 인지된 부채

### ★ 파이프라인 6단계 중 **5단계만 실재한다** — Validation 미구현 (2026-08-15) ★

헌장이 선언한 단계는 여섯이다:

```
Recognition → Extraction → Normalization → Mapping → Validation → Load
                                                      ↑ 이 칸이 비어 있다
```

`validationRules`는 **모든 프로파일이 선언하는데 코드 어디에서도 읽지 않는다.**
타입 선언(`mapping/index.ts:207`)과 머리말 주석 말고는 참조가 0곳이다. 같은 계열이
둘 더 있다 — `extractionRules`의 `headerRowHint`·`sheetSelector`도 전 프로파일에
선언돼 있는데 파이프라인에 넘기는 호출부가 없다.

**이것은 조용한 실패(LOCK 6)가 아니라 «약속한 단계가 없는 것»이다.** 데이터가
틀리게 저장되는 게 아니라 검증이 애초에 안 돈다. 그래서 C 계열(조용한 실패)과
같은 대열에 넣지 않고 여기 적는다 — 섞으면 «고치는 일»의 크기가 뒤섞인다.

> **⚠ 2026-08-19 갱신 — 칸에 첫 입주자가 생겼지만 이 부채는 그대로다.**
> 계획 C가 `core/import/validation/identity.ts`(항등식 증명기)를 그 칸에 넣었다.
> 그러나 그것은 **판정 시 증명**(이 열이 무슨 필드인가)이고, 여기 적힌 부채는
> **적재 시 규칙 실행**(`validationRules`가 선언한 검사를 행마다 돌리는 것)이다.
> 둘은 이름만 같은 다른 일이다 — 증명기가 들어왔다고 이 절을 지우면 안 된다.
> `extractionRules`의 `headerRowHint`·`sheetSelector`도 여전히 호출부 0곳이다.

> **⚠ `signPolicy`는 이 부채가 아니라 잠재 결함이다 — 아래 «4번 대열» 참조.**
> ADR-009 ②(「저장은 양수, 의미는 타입이 정한다」)의 **집행자가 없다.** 선언만
> 있는 것과 규약을 어길 수 있는 것은 무게가 다르다.


### ★ `npm run convert:gate`는 **늘 빨갛다** — 판정은 테스트가 한다 (2026-08-14 확인) ★

`/after-work`로 전 게이트를 돌리다 나왔다. **내가 깨뜨린 것이 아니다** — `af9ad85`
(이번 세션 시작 전) 워크트리에서 돌려 보니 **같은 6개 항목이 이미 빨갛다.**

```
af9ad85     6개 항목 불일치 · 동적 자리 991건 차이
05d37b3     6개 항목 불일치 · 동적 자리 997건 차이   ← 이번에 선언한 이탈만큼 늘었다
9060480     6개 항목 불일치 · 동적 자리 원본 1272 / 출력 1265   (2026-08-19)
```

★ **항목 수(6)는 그대로이고 «동적 자리» 차이만 는다 — 그게 정상이다** ★ CLI는
자리를 **순서대로** 대조하므로, 신설 구간 안에 홀을 하나 더하면 그 뒤가 통째로
한 칸씩 밀려 «불일치»로 세어진다. 2026-08-19에 imp* 홀 4개(`impSheetAutoNote` ·
`impAllSheets` · `impAllSheetsLabel` · `toggleImpAllSheets`)를 더해 차이가 커졌다.
판정하는 쪽(`tests/convert-gate.test.ts`)은 그대로 **12개 전부 통과**하고, 그 안의
«`data-s21` 표식 총수 = `S21_REGIONS.length`» 단언이 «선언 없는 새 구간이 없다»를
지킨다. **숫자가 늘어난 것을 회귀로 읽지 마라 — 항목 6개가 7개가 되면 그때가 회귀다.**

원인은 구조다. CLI(`tools/convert/gate.ts`)는 목업과 출력을 **그대로** 비교하고
**선언된 이탈을 모른다.** `DEVIATIONS`와 `S21_REGIONS`는 `tests/convert-gate.test.ts`
안에 있고, 그 테스트가 이탈을 반영해 대조한 뒤 **12개 전부 통과**한다.

> **§21 패치가 처음 올라간 날부터 CLI는 빨갰다.** gate.ts의 머리말이 그 사실을 이미
> 예고하고 있다 — *"§21 이탈은 별도 커밋에서 의도적으로 만들고, 그 커밋의 diff가 곧
> 목업과 다른 곳의 전체 목록이 된다."*

**왜 그냥 두면 안 되나**: 늘 빨간 게이트는 **사람을 훈련시켜 게이트를 무시하게
만든다.** 새 세션이 «전 게이트 확인»을 하다 이걸 만나면 없는 사고를 좇거나,
반대로 «원래 빨간 거»라며 진짜 회귀까지 넘기게 된다.

**처방(미착수)**: 이탈 선언(`DEVIATIONS`·`S21_REGIONS`)을 테스트에서 **모듈로 빼서
CLI와 공유**한다. 그러면 CLI가 테스트와 같은 답을 내고 둘 다 녹색이 된다.
지금 안 하는 이유는 이 세션의 범위 밖이고, 잘못 옮기면 **게이트의 보증 범위가
조용히 넓어지는** 변경이라 그 자체로 검토가 필요해서다.

**그동안의 규율**: 보존 게이트의 판정은 `npm test`가 한다. `convert:gate` CLI는
«선언되지 않은 이탈이 몇 건인지» 세는 참고 도구로만 읽는다.

### ★ 위저드 v1은 파이프라인을 **메인 스레드**에서 돌린다 — ADR-001 조건 2 위반 ★

조건 2는 «파싱은 Worker에서»다. 위저드 v1은 그러지 않는다. **조용한 위반과 기록된
부채는 다르므로** 여기 적는다.

```
실전 기준으로 순서를 정했다 — 8월 정산 파일은 수백 행이라 문제가 없다
그러나 쿠팡 광고(8만 행 · ~10MB)는 스모크 실측으로 9.5~10.4초 화면이 멈춘다
```

그동안의 방어는 **고지 하나**다 (`data-s21="import-big"` · 4MB 이상):

> *"큰 파일입니다. 가져오는 동안 화면이 잠시 멈출 수 있습니다 — 앱이 멈춘 것이
> 아니라 파일을 읽고 있는 중입니다. 창을 닫지 말고 기다려 주세요."*

**고지 없는 10초 프리즈는 강제 종료를 부른다.** 사용자의 자연스러운 반응이
"죽었나?"이기 때문이다. 미리 말하면 같은 10초가 "크니까 그렇구나"가 된다.
고칠 수 없는 것을 숨기지 않는 것이 지금 할 수 있는 정직이다.

**해소 조건**: Worker 이관. 그때 `import-big` 구간과 `BIG_FILE_BYTES` 상수를
함께 지운다 — 임시물의 수명을 §21-7과 같은 방식으로 묶어 둔다.

---

## 막혀 있는 것

### 1. ~~합격 기준 2 — 메모리~~ → 통과. 다만 여유가 얇다

`NormalizedValue` 평탄화를 실행했다 ([ADR-007](ADR-007-정규화-표현.md)).
셀당 `{kind, value, raw}` 객체 344만 개 → **청크당 평면 배열 3개**.

```
12회 연속 — 195.2 · 211.2 · 217.4 · 228.9 · 230 · 235.3
            237 · 238.2 · 242.6 · 243 · 248.5 · 249.7
중앙값 236.2 · 최대 249.7 / 256MB     12회 전부 통과
시간   4.9~5.0초 / 30초
행 수  80,137 = 80,137 = 80,137
```

개정 전은 `233 · 244.8 · 249 · 267.3`으로 4회 중 1회 초과였다.

**벽이 옮겨갔다.** 단계별 재측정:

```
파싱만                  ~205MB      ← 새 벽. 예산의 80%
+ 정규화 + 매핑         ~207-218MB  (평탄화 이전 245MB → 사실상 0)
+ SQLite 적재           ~246MB
                        ────────
예산                     256MB
```

정규화는 이제 공짜다. ADR-003이 "JS 파이프라인 232MB"라 묶어 부른 것의 대부분은
**파싱이었다.** 다음에 메모리를 더 줄여야 하면 손댈 곳은 파서다 — 정규화가 아니다.

**정직하게**: 여유 1.7MB에 관측 폭 59.3MB(195.2~254.3)다. 다음 회차가 넘을 가능성을
배제할 수 없다. 세션 3에서 실파일이 늘면 다시 볼 자리다.

> **갱신 (2026-08-15)** — C 계열 이후 4회는 `219.2 · 244.2 · 254.3 · 251.9`다. 최대가
> 249.7에서 **254.3으로 올라가 관측 폭이 195.2~254.3이 됐다.** 회귀로 보지 않는
> 근거는 «옛 폭 안»이 **아니라**(254.3은 250 밖이다) «예산 안 + 구조적 무접촉»이다.
> 3회로 추세를 만들지 않는다 — 다만 **넘는 회차가 나오면 그때 팔 곳은 파서**다.

재현: `npm run e2e` · `SKIP_LOAD=1 npm run e2e`(적재 제외) · `npm run perf`(파싱만)

### 2. ~~ADR-003 — Rust 미설치~~ → 해소

Rust 1.97.1 stable-msvc + MSVC Build Tools 설치 완료. 링크 확인됨.
[ADR-003](ADR-003-sqlite-바인딩.md) 확정 — **rusqlite를 Tauri 커맨드 뒤에.**

`tauri-plugin-sql`을 버린 이유는 성능이 아니라 **커맨드 표면**이다. 플러그인은
프론트엔드가 임의 SQL을 실행하게 해서, 합격 기준 4("전체 테이블 적재 경로 없음")를
구조로 증명한 것이 무의미해진다.

적재 처리량은 rusqlite가 약 3배(1,700~1,950ms → 545~692ms). 벤치는 `bench/sqlite/`
(저장소 안 — 다른 기기에서도 재현된다. Rust 툴체인 필요).

> **`cargo`가 PATH에 없다.** rustup은 `~/.cargo/bin`에 깔리는데 셸(Git Bash·PowerShell)
> 어느 쪽에서도 안 잡힌다 — 설치 후 셸을 다시 열지 않았거나 PATH 갱신이 안 된 것이다.
> 시스템 PATH는 기기 전역 설정이라 프로젝트가 요구할 일이 아니므로, **전체 경로로 부른다**:
>
> ```bash
> "$USERPROFILE/.cargo/bin/cargo.exe" run --release
> ```
>
> 확인된 버전: cargo 1.97.1 · rustc 1.97.1 · stable-x86_64-pc-windows-msvc.
> `npm test`·`npm run e2e`는 Rust와 무관하므로 영향 없다.

### 3. ~~Tauri 창이 화면에 뜨지 않는다~~ → **오진이었다. 실측으로 기각** (2026-08-13)

1단 스크린샷을 찍다가 "앱은 도는데 창이 안 보인다"를 만났고, 처음엔 제품 결함으로
적었다. **틀렸다.** 실측이 두 가지를 갈라줬다.

**① `-16000 @ 157x25`의 정체 — 그냥 최소화된 창이다.**

```
+0s   2586x1656 @ (294,129)      vis=T  iconic=False   창 생성
+2s   2586x1656 @ (147,29)       vis=T  iconic=False   fit 적용 · 포커스
+4s   314x50    @ (-32000,-32000) iconic=True          ← 최소화
```

`-32000 @ 314x50`은 Windows가 **최소화된 창을 주차하는 표준 자리**다. 내 열거
스크립트가 DPI 비인식이라 200% 배율에서 절반으로 보여 `-16000 @ 157x25`가 됐다 —
**두 값은 같은 것이었다.** 도구가 만든 착시를 새 현상으로 읽은 것이다.

**② 최소화의 범인은 실행 방식이었다.**

```
에이전트 셸에서 Start-Process   →  +4s에 최소화된다
explorer.exe로 실행 (정상 실행)  →  90초 내내 정상. 상태 변화 0
```

우리 코드에 `minimize` 호출은 프론트·Rust 통틀어 **0건**이다(grep). 즉 앱이
아니라 **비대화형 셸에서 띄운 프로세스의 포커스 처리**가 만든 현상이었다.

> **교훈은 도구 쪽이다.** 앞으로 앱을 눈으로 확인할 때는 `explorer.exe <exe>`로
> 띄운다. `Start-Process`로 띄우고 "창이 안 뜬다"고 판정하면 없는 버그를 좇게 된다.
> 그리고 **측정 도구의 좌표계를 먼저 의심한다** — DPI 비인식 스크립트가 200%
> 기기에서 내놓는 숫자는 절반이다.

**다만 이 소동이 진짜 버그 하나를 잡았다** — `fit_to_work_area`. 아래 참조.

### ★ 그 뒤 더 정확해졌다 — 검증층의 경계선 (2026-08-13) ★

정산 화면을 스크립트로 눌러 보려다 재현 조건이 좁혀졌다:

```
explorer.exe로 띄우고 **가만두면**            90초 내내 정상
SetForegroundWindow·클릭을 보내면             최소화된다 (재현됨)
```

즉 **앱을 띄워 보는 것은 되고, 스크립트로 조작하는 것은 안 된다.** 대시보드 이정표를
진짜 창으로 찍을 수 있었던 것은 조작이 필요 없는 첫 화면이었기 때문이고, 정산·주문은
사이드바 클릭이 필요해 못 찍었다.

> **그래서 검증층이 이렇게 갈렸다 — 이게 지금의 작업 방식이다.**
>
> | 층 | 누가 | 도구 |
> |---|---|---|
> | 마크업·데이터가 맞나 | 에이전트 | `tools/harness/render-screen.ts` (SSR · 자체 완결 HTML) |
> | **클릭 동선이 되나** | **사람** | 앱을 띄워 직접 눌러본다 |
>
> SSR 렌더는 **같은 Template · 같은 CSS · 같은 조회**라 마크업과 숫자는 증명하지만
> **상호작용은 증명하지 못한다.** 그 층을 SSR로 대신했다고 적지 않는다.

**사람이 앱을 띄우는 법** — `Start-Process`가 아니라 탐색기에서 실행한다:

```powershell
npm run dev     # 먼저 Vite (별도 창)
# 그다음 탐색기에서 src-tauri\target\debug\app.exe 더블클릭
```

---


---

## 기기를 오가며 작업한다

이 저장소는 데스크톱 3대 · 노트북 · 핸드폰에서 번갈아 열린다. 그래서
**세션의 시작과 끝을 저장소 안 명령으로 고정**해뒀다.

| | |
|---|---|
| `/before-work` | 원격을 당겨오고 · 의존성을 맞추고 · 정상인지 확인하고 · 어디서부터인지 보고 |
| `/after-work` | 검증하고 · 커밋하고 · 푸시하고 · (필요하면) PR 만들고 · 이 문서를 갱신하고 · **닫혔는지 확인** |

둘 다 `.claude/commands/`에 있다 — **저장소 안이라 clone하면 어느 기기에서든 뜬다.**
`~/.claude/commands/`에 두면 그 기기에서만 되므로 여기 둔 것이다.

**기기를 바꿀 때는 `/after-work`로 닫고 옮긴다.** 닫지 않고 옮기면 다음 기기에서
`/before-work`가 "원격이 뒤처져 있다"거나 갈라진 상태를 보고하게 되고, 그때는
합치는 방법을 물어본다 — 자동으로 rebase하지 않는다.

---

## 새 기기에서 이어받기

```bash
git clone https://github.com/peterkwon248/FlowBase-Vertical.git
cd FlowBase-Vertical
npm ci          # package-lock의 integrity 해시로 SheetJS 타르볼까지 검증된다

npm run state   # ★ 이 기기가 어디에 서 있나 — DB가 없으면 «(파일이 없다)»라고 말한다

# ★ 화면 단언 33건을 깨우려면 개발 DB를 먼저 만든다 (2026-08-20 · B-2) ★
npx tsx tools/harness/pnl.ts --clean        # 커밋된 비식별화 픽스처로 .tmp/pnl.sqlite

npm test        # 1,296 passed / 1 skipped (2026-08-21 · 96파일 · node v24.13.0 · win32)
```

> ★ **브라우저(`npm run dev`)로 열려면 데모 DB를 따로 만든다** ★
> `public/demo.sqlite`도 **gitignore라 clone에 안 따라온다.** 없으면 앱이 **빈 DB로 뜨고**
> `data.ts:237`이 `console.warn` 하나만 남긴다 — 화면은 「웹 데모」 배지를 단 채 텅 빈다
> (조사 1.10이 이 자리다).
>
> ```bash
> npm run demo-db     # public/demo.sqlite 생성. `npm run build`의 prebuild가 이걸 부른다
> ```
>
> **`npm run dev`는 이것을 안 부른다** — `prebuild`는 `npm run build`에만 붙는다.
> 그래서 clone 직후 `npm run dev`부터 켜면 빈 앱을 보게 된다.
>
> ⚠ **브라우저는 실 데이터를 못 본다.** 판정은 URL이 아니라 `data.ts:209`
> `isWebDemo() = window.__TAURI_INTERNALS__ === undefined` 한 줄이다 —
> 같은 5173이라도 **Tauri 창 안에서 열면 실 DB**, Chrome에서 열면 데모다.
> 화면 우하단에 「웹 데모 · 예시 데이터」 배지가 있으면 데모 쪽이다.
>
> **실 데이터로 앱을 보려면** (`npm run dev`가 떠 있으면 먼저 끈다 — `strictPort: true`):
>
> ```bash
> npm run app     # = tauri dev. HMR이 살아 있는 채로 실 DB(.tmp/pnl.sqlite)를 연다
> ```
>
> 바탕화면 바로가기/기존 `app.exe`는 **빌드 시점 화면이 구워져 있다**(`frontendDist`) —
> 오늘 고친 것을 보려면 `npx tauri build --no-bundle`로 다시 구워야 한다.

> **화면 테스트는 `.tmp/pnl.sqlite`가 있을 때만 돈다** (gitignore라 새 기기엔 없다).
> 없으면 조용히 건너뛰고 **1,151 → 1,118**이 되는데 그것도 통과이긴 하다. 다만
> LOCK 10 화면 집행·손익 3층 항등식·「화면 숫자 = CLI 숫자」가 **전부 자는 상태**라,
> 위 한 줄을 먼저 돌리는 것이 낫다. CI도 그렇게 한다.
>
> 남는 1 skip은 `#8 원본`이다 — `fixtures/raw/`가 커밋되지 않으므로 **영원히** 건너뛴다.

> ⚠ **DB가 이미 있는데 `npm test`가 무더기로 깨지면 코드가 아니라 DB를 먼저 의심한다.**
> 스키마는 파일에 남고 마이그레이션은 밀린다 — 아래 `migrate-db.ts`.
> 2026-08-19에 이 기기가 버전 8에 멈춰 29건이 깨졌다(`no such table: overhead`).
> **지금 정본은 14다** (014 ad-listing).
>
> ★ **DB가 이미 있는 기기라면 마이그레이션부터 따라잡는다** ★
>
> ```bash
> npx tsx tools/harness/migrate-db.ts     # 밀린 것만 적용 · 연결·SKU 보존
> ```
>
> `pnl.ts`는 DB를 **새로 만들어** 사람이 만든 연결 79개와 SKU 61개를 지운다.
> 스키마가 밀렸을 때 쓸 것은 이쪽이다 (앱은 열 때 스스로 따라잡는다).

Node 22.5+ 필요 — `node:sqlite`(저장 계층)와 `TextDecoder('euc-kr')`(인코딩 감지)를 쓴다.

`bench/sqlite`를 돌리려면 Rust 툴체인이 추가로 필요하다 (없어도 `npm test`·`npm run e2e`는
전부 동작한다). 이 기기는 설치돼 있고, 새 기기에서는:

```bash
winget install --id Rustlang.Rustup                      # Windows
winget install --id Microsoft.VisualStudio.2022.BuildTools --override "--quiet --wait --norestart --add Microsoft.VisualStudio.Workload.VCTools --includeRecommended"
```

Build Tools는 UAC 승인이 필요하다 — `Start-Process -Verb RunAs`로 띄우면 클릭 한 번으로 된다.

### 그대로 되는 것

`npm test` · `npm run harness` · `npm run perf` · `npm run e2e` · `npm run dev` ·
`npm run build` 전부 동작한다. 테스트와 하네스는 기본으로 `fixtures/clean/`(커밋됨)을
보기 때문이다.

**단 `npx tsx tools/harness/pnl.ts`는 기본이 `fixtures/raw/`다** (실파일 관통이
목적이라). 새 기기·CI에는 원본이 없으므로 **`--clean`을 붙인다** — 그러면 커밋된
비식별화본으로 돌고, 화면 테스트가 쓰는 DB로는 그것으로 충분하다(단언이 절대값이
아니라 **불변식**이라서다).

### 따라오지 않는 것

| | 왜 | 없으면 |
|---|---|---|
| `fixtures/raw/` (15.6MB) | 실명·연락처·주소 포함, gitignore (헌장 E-10) | `tools/deident/*` 실행 불가. `#8 원본` 테스트는 자동 skip되므로 `npm test`는 정상 통과 |
| `_ref/` | 스크린샷·대행사 리포트, 앱 코드 아님 | 참고 자료만 없음. 코드·문서에 영향 없음 |
| **`_ref/원가/`** | 원가표 3종(커닝페이퍼·원가시트 병합본·SKU별 입력표) — gitignore | ★ **`cost-import.ts`가 못 돈다** ★ 그 도구는 `_ref/원가/SKU별-입력표.tsv`를 읽는다. 새 기기에서 원가를 다시 넣으려면 그 파일부터 옮겨야 한다 (사업 데이터라 저장소에 안 넣는다) |
| **`~/.claude/commands/`** | 기기 로컬 슬래시 명령 | ★ **2026-08-21에 사고가 났다** ★ 여기 `before-work.md`·`after-work.md`의 낡은 사본이 있었고 **저장소판(`.claude/commands/`)을 덮고 있었다.** 3개월 전 다른 프로젝트용이라 없는 파일 다섯을 찾게 했고, 「코드 충돌은 origin/main 우선」이라는 **자동 병합 지시**까지 들어 있었다(저장소판이 의도적으로 금지한 것). 지웠다 — 명령이 이상하면 `ls ~/.claude/commands/`부터 본다.<br>★ **지운 것은 «이 기기»의 사본뿐이다** ★ git이 저장소 밖을 못 건드리므로 **다른 기기의 사본은 그대로 남아 있다.** 노트북·다른 데스크톱에서 처음 `/before-work`를 치기 전에 그 명령부터 돌린다. 저장소판이 떴는지는 명령 목록의 **description**으로 판별한다(저장소판 = 「작업 시작 — 원격을 당겨와…」) |
| Claude 메모리 | `~/.claude/projects/.../memory/`는 기기 로컬 | 작업 방식 메모 2건. 내용은 ADR 규율로 저장소에 반영돼 있다 |
| 태스크 목록 | 세션 로컬 | 이 문서가 대체한다 |
| `dist/` · `.tmp/` | 빌드·측정 산출물, gitignore | `npm run build`·`npm run e2e`가 다시 만든다 |
| **`public/demo.sqlite`** | 브라우저(웹 데모)가 `fetch`로 가져가는 DB · gitignore | ★ **없으면 `npm run dev`가 빈 앱으로 뜬다** ★ `data.ts:237`이 `console.warn` 하나만 남기고 화면은 「웹 데모」 배지를 단 채 텅 빈다(조사 1.10). **`npm run demo-db`로 만든다.** `npm run dev`는 이걸 **안 부른다** — `prebuild`는 `npm run build`에만 붙어 있다 |
| **`.tmp/pnl.sqlite`의 내용** | 사용자가 앱으로 실제로 넣은 데이터 | ★ **`pnl.ts`가 재현하지 못한다** ★ 그 CLI는 픽스처 **4종**만 넣는다(2026-08-20에 쿠팡 제트 #15가 늘었다 — 광고비 배분이 붙을 리스팅을 만들려고) — **쿠팡 매출 2종(79M)·사람이 만든 연결 79개·SKU 61개는 앱에서 다시 넣어야** 나온다. 새 기기에서 화면 숫자가 다른 것은 그 때문이고 결함이 아니다 |
| **그 DB의 «품목»과 «이중 기록»** | 둘 다 **적재 시점에** 생긴다 | 이 기기의 DB는 두 기능 **이전에** 들어온 batch뿐이라 품목 0행이고 ESM 클레임도 주문에 없다. 화면이 「품목 없이 들어온 주문 N건」이라고 말하는 것이 정상이며, **7월 파일 3종을 다시 넣으면** 해소된다(UPSERT라 행은 안 늘고 매출은 +388,700 움직인다) |
| `gh` 인증 | 기기 로컬 | 푸시는 Git Credential Manager가 따로 처리한다. `gh`는 PR 상태 표시용이며 없어도 작업에 지장 없다 — 필요하면 `gh auth login` |
| **DB 스키마 버전** | `.tmp/pnl.sqlite`는 gitignore이고 마이그레이션은 **파일에 남는다** | ★ **DB가 이미 있는 기기는 반드시 따라잡아야 한다** ★ 2026-08-19에 이 기기가 8에 멈춰 있어 `npm test`가 29건 실패했다(`no such table: overhead`). 처방은 위의 `npx tsx tools/harness/migrate-db.ts` — 코드가 깨진 게 아니라 DB가 낡은 것이니 **테스트 실패를 먼저 이걸로 의심한다** |
| `.tmp/pnl-backup-*.sqlite` · `.tmp/pnl.sqlite.bak-*` | 마이그레이션 전에 뜬 백업 · gitignore | 없어도 무관. 이 기기에는 `pnl-backup-2026-08-19-premigrate.sqlite`(마이그레이션 8 시점)와 `pnl.sqlite.bak-14`(2026-08-21 · 15→16 적용 전)가 있다. **며칠 앱을 써 보고 이상 없으면 지운다** — `rm .tmp/pnl.sqlite.bak-14` |
| Vercel 배포 상태 | 저장소 밖 | `main`에 푸시하면 자동 재배포된다. **배포별 고정 주소(`…-htk49re5a-…`)는 그 시점 빌드에 영구 고정**이라 새로고침해도 안 바뀐다 — 늘 `flow-base-vertical.vercel.app`으로 연다 |

원본 픽스처가 필요하면 사용자의 `플로우베이스용 엑셀 자료.zip`을 `fixtures/raw/`에 푼다.
비식별화본을 다시 만들려면 그다음 `npx tsx tools/deident/apply.ts` → `verify.ts`.

---

## 명령어

```bash
npm test                                    # 1,151개 (2026-08-20) · 89파일
npm run typecheck
npm run dev                                 # 앱 (http://localhost:5173)
npm run build                               # typecheck + vite build
npm run harness                             # expected 매니페스트 재생성
npm run perf                                # 세션 1 게이트 (파싱만)
npm run e2e                                 # 세션 2 게이트 (적재까지)

# ★ 저장소 사실을 문서에 기계가 적는다 (2026-08-21 신설) ★
#   배선/컷·스키마처럼 **기기와 무관한** 숫자만 다룬다. 기기 사실(DB 행 수)은 state 쪽이다.
npm run facts                               # 찍어만 본다
npm run facts -- --write                    # docs/작업-상태.md의 <!-- facts:… --> 마커를 갈아끼운다
#   판정자는 tests/doc-facts.test.ts 이고 npm test 안에서 돈다. 만료(expires) 태그도 거기서 문다

# ★ 게이트 자가시험 — «가드가 아직 물 줄 아는가» (2026-08-21 신설)
#   등재된 흠집을 하나씩 넣고 그 가드가 **붉어지는지** 본다. 초록이면 가드가 죽은 것이다.
#   느리다(케이스마다 시험 한 벌) — Stop 훅에 안 걸린다. **가드를 만들거나 크게 고친 뒤**에 돈다.
npm run gates:selftest                      # 전부 (12건 · 2026-08-21 기준)
npm run gates:selftest -- cut-stale         # 하나만
#   두 실패를 구별한다: 「자해를 넣었는데 통과했다」(가드가 죽음)
#                    「흠집 자리를 못 찾았다」(케이스가 낡음 — 가드는 멀쩡할 수 있다)

# ⚠ npm run convert:gate 는 **빨간 것이 정상이다** — 아래 절 참조.
#   보존 게이트의 진짜 판정은 tests/convert-gate.test.ts 이고 npm test 에 들어 있다.

# 정합성 게이트 — 표현을 바꾸는 리팩터링 전후로 "같은 답"을 증명한다 (ADR-007)
npx tsx tools/harness/equivalence.ts --save   # 고치기 전에 기준선을 뜬다
npx tsx tools/harness/equivalence.ts --check  # 고친 뒤 대조. 452만 셀 + 매핑 결과

npx tsx tools/harness/dump-sheet.ts 6 0     # 픽스처 원시 행 (열은 기본 전체)
npx tsx tools/harness/check-excluded.ts 4 3 # 무엇이 왜 제외됐나
npx tsx tools/harness/classify-check.ts 3   # 이름 힌트 유무 분류 비교
npx tsx tools/harness/verify-fast-path.ts 13

# 3b-0 실파일 관통 — DB(.tmp/pnl.sqlite)를 만든다. 화면 테스트가 이걸 쓴다
npx tsx tools/harness/pnl.ts                # 손익 5줄 + "담지 못한 것" + 리스팅 적재
npx tsx tools/harness/pnl.ts --clean        # ★ 새 기기·CI는 이쪽 ★ fixtures/raw가 없으므로
                                            #   기본값(raw)으로 돌리면 파일을 못 찾는다

# 배선 — CLI도 판정할 수 있다 (2026-08-20 · B-5). --check 없이는 리포트다
npx tsx tools/harness/wiring.ts --check     # 선언되지 않은 미배선이 있으면 exit 1

# 화면을 실데이터로 렌더해 눈으로 본다 (.tmp/<뷰>.html — 자체 완결, 외부 자원 0)
npx tsx tools/harness/render-screen.ts dash
npx tsx tools/harness/render-screen.ts settlement
npx tsx tools/harness/render-screen.ts orders
npx tsx tools/harness/render-screen.ts linking                       # 탭 기본 todo
npx tsx tools/harness/render-screen.ts linking .tmp/pnl.sqlite done  # DB·탭 지정

# 렌더 산출물을 브라우저로 본다 (file://을 못 여는 도구용 최소 정적 서버)
npx vite --config vite.harness.config.ts     # → http://localhost:5199/linking.html

# 리스팅 매칭 임계값의 근거 — 새 채널이 붙으면 다시 떠서 절단점을 재확인한다
npx tsx tools/harness/listing-match-dist.ts
```

> **`.tmp/pnl.sqlite`가 없으면** 화면 테스트 **3종**(`dashboard-wiring`·`settlement-rows`·
> `order-rows`)이 **조용히 통과하지 않고 건너뛴다** — 19개가 빠져 `npm test`가 **444**로
> 나온다. 그때는 `npx tsx tools/harness/pnl.ts`를 먼저 돌린다 (원본 픽스처 필요).
>
> ★ **`linking-screen`·`screen-safety`는 더 이상 여기 매여 있지 않다** ★
> 커밋된 픽스처에서 `runImport`로 **자기 세계를 짓는다.** 사용자가 2d에서 리스팅
> 86개를 전부 연결하자 콜드스타트를 전제한 단언이 깨진 사건이 계기다 —
> **개발용 DB는 사람이 제품을 쓰면 변하고,** 그걸 고정된 세계로 읽는 테스트는
> 사용자가 앱을 쓸 때마다 빨개진다. 빨간 게이트는 곧 꺼진다.
> `import-run`·`import-analyze`·`profile-registry`도 같은 방식이다.

---


---

## 여기까지다 — 이 아래는 없다

과거 세션 기록은 아카이브에 있다. **읽지 않아도 된다.**

| 문서 | 무엇 |
|---|---|
| [작업-로그-2026-08-후반](작업-로그-2026-08-후반.md) | 08-15 ~ 08-21 |
| [작업-로그-2026-08-전반](작업-로그-2026-08-전반.md) | 08-12 ~ 08-14 |
| [작업-리듬](작업-리듬.md) | 어떻게 일하는가 (상태가 아니다) |

> 2026-08-20 정리 전에는 이 문서가 **5,344줄**이었고 「지금 어디인가」가 2,615줄에
> 있었다. 새 세션은 과거 2,600줄을 지나야 현재에 닿았다. 그 순서를 뒤집은 것이
> 이 정리다 — **이 문서는 짧게 유지한다.** 세션 요약은 직전 1개만 남기고
> 나머지는 로그로 내린다.

