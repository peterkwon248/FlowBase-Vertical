# 의존성
node_modules/
.pnp
.pnp.js

# 빌드 산출물
dist/
build/
out/
.next/
.nuxt/
.output/
.turbo/
.vercel/

# 환경 변수
.env
.env.*
!.env.example

# 로그
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 테스트 / 캐시
# ★ 반드시 루트 기준이어야 한다 ★ `coverage/`라고만 쓰면 **모든 깊이**에 적용되어
# `src/core/coverage/`(§22 판정 모듈)가 통째로 무시된다. 실제로 그렇게 돼 있었고,
# 다른 기기에서 clone하면 import가 깨졌을 자리다. 도구 산출물은 루트에만 생긴다.
/coverage/
.cache/
*.tsbuildinfo

# 에디터
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# 사용자 참고 자료 (스크린샷·대행사 리포트) — 앱 코드 아님, 용량 큼
_ref/

# 픽스처 원본 — 비식별화 전 커밋 금지 (헌장 E-10)
fixtures/raw/
*.원본.xlsx
*.원본.csv

# 종단 게이트가 만드는 임시 DB
.tmp/

# 웹판이 동봉하는 데모 DB — `npm run demo-db`가 픽스처로 만든다 (빌드 산출물)
public/demo.sqlite
